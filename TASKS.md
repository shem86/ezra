# Tasks: Household AI Agent v1 (hh-assistant)

> **Status: APPROVED 2026-06-09 — execution in progress.**
> Breakdown of `PLAN.md` (approved 2026-06-09). Tasks are dependency-ordered within each milestone; each is sized for one focused session and touches ≤ ~5 files. M0–M3 are fully specified; M4–M6 tasks are named and gated but get their full acceptance criteria refined at milestone entry (their shape depends on what M1/M3 validate). `[H]` marks tasks the builder does by hand (drills, approvals, external accounts). Number acquisition, warming, and soak testing are out of scope; tasks assume a paired, usable WhatsApp number.

## M0 — Scaffold

- [x] **T1: Repo + TypeScript scaffold** *(done 2026-06-09)*
  - Acceptance: pnpm project with `save-exact` enforced via `.npmrc`; strict `tsconfig.json`; `src/index.ts` placeholder; `pnpm build` compiles.
  - Verify: `pnpm build`.
  - Files: `package.json`, `.npmrc`, `tsconfig.json`, `src/index.ts`.

- [x] **T2: Vitest wiring** *(done 2026-06-09)*
  - Acceptance: `pnpm test` runs a passing smoke test; unit/integration split configured (integration suite skipped when DB env is absent).
  - Verify: `pnpm test`.
  - Files: `vitest.config.ts`, `tests/unit/smoke.test.ts`, `package.json`.

- [x] **T3: ESLint baseline** *(done 2026-06-09)*
  - Acceptance: flat-config ESLint with TS support; `pnpm lint` clean on scaffold; rule slot reserved for the custom determinism rule (T9).
  - Verify: `pnpm lint`.
  - Files: `eslint.config.js`, `package.json`.

- [x] **T4: Dev database (single Postgres + pgvector)** *(done 2026-06-09)*
  - Acceptance: `docker compose up -d` starts one Postgres with pgvector; a connect-and-`CREATE EXTENSION vector` smoke check passes. Note in `infra/README.md`: local runtime is Colima on the dev Mac; CI (Linux) is the arbiter.
  - Verify: `pnpm test` integration smoke against the dev DB.
  - Files: `docker-compose.yml`, `infra/README.md`, `.env.example`, `tests/integration/db-smoke.test.ts`.
  - Depends on: T2.

- [x] **T5: CI pipeline** *(done 2026-06-09: repo pushed to github.com/shem86/hh-assistant, CI green on main. Caveat: branch protection requires GitHub Pro or a public repo — "red CI blocks merge" is unenforced until the builder upgrades or makes the repo public.)*
  - Acceptance: GitHub Actions running `pnpm build && pnpm lint && pnpm test` with a Postgres+pgvector service container; red CI blocks merge.
  - Verify: CI green on a pushed branch.
  - Files: `.github/workflows/ci.yml`.
  - Depends on: T1–T4.

- [x] **T6: Config and secrets loading** *(done 2026-06-09)*
  - Acceptance: env-based config module, Zod-validated at startup with clear failure messages; placeholders for Anthropic key, Langfuse keys, DB URL, alert-channel token; no secret ever read outside this module.
  - Verify: unit tests (valid env passes, missing/invalid env fails loudly).
  - Files: `src/ops/config.ts`, `tests/unit/config.test.ts`, `.env.example`.
  - Depends on: T1, T2.
  - **Gate M0 complete:** CI green end-to-end.

## M1 — De-risking spikes (T7–T9 parallelizable)

- [x] **T7: Prompt-caching spike (SPEC Phase-0 gate)** *(done 2026-06-09: PASS, cache_read_input_tokens=6135 through AI SDK passthrough; no escape hatch — see docs/spike-results.md)*
  - Acceptance: script calls Claude twice via AI SDK Core with `cache_control` on a stable prefix through provider passthrough; usage fields captured; result (cache_read tokens > 0, or failure) written to `docs/spike-results.md`. On failure: stop, surface the `@anthropic-ai/sdk` escape-hatch decision to the builder before M4.
  - Verify: run `spikes/cache-control.ts` twice; read the recorded usage numbers.
  - Files: `spikes/cache-control.ts`, `docs/spike-results.md`, `package.json`.
  - Depends on: T6.

- [x] **T8: DBOS semantics spike (SPEC Phase-0 foundation)** *(done 2026-06-09; pinned @dbos-inc/dbos-sdk 4.19.8 — see docs/spike-results.md)*
  - Acceptance: committed integration test proving, against the dev Postgres: (1) transactional step = state write + step record atomic; (2) kill-mid-flight then recover replays to identical output with no double effect; (3) queue concurrency-1 FIFO; (4) a scheduled workflow fires; (5) journal/state/pgvector co-reside in one Postgres. DBOS version pinned to what this validates.
  - Verify: `pnpm test` (spike suite); kill/replay test green.
  - Files: `spikes/dbos/spike.ts`, `tests/integration/dbos-spike.test.ts`, `package.json`, `docs/spike-results.md`.
  - Depends on: T4.

- [x] **T9: Determinism ESLint rule** *(done 2026-06-09)*
  - Acceptance: custom rule bans `Date.now`/`new Date`, `Math.random`, `process.env` reads, and direct I/O calls inside `@DBOS.workflow` function bodies; red on violation fixtures, green on clean fixtures; wired into `pnpm lint` as CI-failing.
  - Verify: rule's own fixture tests + `pnpm lint`.
  - Files: `eslint-rules/no-nondeterminism-in-workflow.ts`, `eslint-rules/no-nondeterminism-in-workflow.test.ts`, `eslint.config.js`.
  - Depends on: T3.

- [x] **T10 [H]: M1 gate review** *(closed 2026-06-09: builder accepted — caching confirmed, DBOS 4.19.8 pin accepted; see docs/spike-results.md)*
  - Acceptance: builder reads `docs/spike-results.md`; caching confirmed or escape hatch decided; DBOS version pin accepted.
  - Verify: gate recorded in the doc with a date.
  - Depends on: T7, T8, T9.

## M2 — Transport adapter + ops monitoring (agent-free)

- [x] **T11: Transport interface + Baileys connection** *(done 2026-06-09 — baileys 7.0.0-rc13 reviewed+pinned (docs/dep-reviews/), adapter unit-tested via injected socket, session store with re-pair-only recovery; builder verified manual QR pairing AND restart-reconnect-without-re-pair via `pnpm pair` same day. Running on builder's personal number — echo suppression is sent-id-based, not fromMe (see docs/pairing.md))*
  - Acceptance: `Transport` interface (connect, send, onMessage, onStateChange, forceReconnect) that M3's stub and M6's real adapter both implement; Baileys connects with session state persisted to a configurable writable dir; QR pairing flow documented; session dir is gitignored and never backed up for restore (re-pair on loss).
  - Verify: manual pairing with the project's WhatsApp number (provisioning out of scope); reconnect after process restart without re-pairing.
  - Files: `src/transport/types.ts`, `src/transport/baileys.ts`, `src/transport/session-store.ts`, `docs/pairing.md`.
  - Depends on: T6; needs a paired WhatsApp number available.

- [x] **T12: Health monitoring + independent alerting + dead-man ping** *(done 2026-06-10: Telegram alert channel (plain Bot API fetch, no new dep, token-redacted errors); health monitor 60s-grace down-alerts / immediate logged-out / recovery, one alert per outage; dead-man GET pinger, passive error handling; config requires ALERT_CHANNEL_CHAT_ID + DEADMAN_PING_URL. 19 unit tests. Builder created the bot + healthchecks.io check and verified the Telegram channel end-to-end (bot token rotated after a WhatsApp self-send leak). The kill-drills in this task's verify line — socket kill → alert, process kill → dead-man fires — are identical to T14's drill content and are executed/logged there)*
  - Acceptance: socket-state monitor emitting down-alerts over a non-WhatsApp channel (Telegram bot unless builder objects at T10); scheduled dead-man ping to an external check service; both configurable via T6 config.
  - Verify: unit tests for state transitions; manual: kill socket → alert arrives; stop process → external dead-man fires.
  - Files: `src/ops/health.ts`, `src/ops/alerts.ts`, `src/ops/deadman.ts`, `tests/unit/health.test.ts`.
  - Depends on: T11.

- [x] **T13: Standalone transport runner** *(done 2026-06-10: `pnpm transport` wires Baileys + T12 health/Telegram/dead-man + stdin commands (send <jid> <text> with 1.5–4.5s human jitter, reconnect, status, help, quit); refuses to start unpaired; command core unit-tested against a fake transport; runner-cli.ts entry follows the pair-cli pattern; loadTransportOpsConfig means no LLM/DB keys needed. Builder dry-run verified same day: connect, inbound logging, manual send arrived in the group. Bonus finding: sends to @lid JIDs deliver — see docs/pairing.md)*
  - Acceptance: standalone runnable (`pnpm transport`) that connects, monitors, supports a manual test send (human-like delay jitter) and a forced-reconnect command. No LLM, no DB writes.
  - Verify: dry-run locally; one manual test send arrives in the group.
  - Files: `src/transport/runner.ts`, `package.json`.
  - Depends on: T11, T12.

- [x] **T14 [H]: M2 operational drill** *(done 2026-06-10: all 5 drills pass — forced reconnect, network kill (caught and fixed a real monitor bug: down-state keyed on 'closed' which the adapter never emits mid-retry; regression-tested), device-logout 🚨 (simulated ban → immediate alert), kill -9 → dead-man fired ~2 min, restart recovered. Results in docs/ops-drills.md. M2 complete)*
  - Acceptance: on the running transport runner — socket kill produces an alert on the independent channel; process kill trips the dead-man within 2× ping interval; forced reconnect recovers cleanly. Results logged.
  - Verify: manual drill; results in `docs/ops-drills.md`.
  - Depends on: T13.

## M2.5 — Host + backups (parallel to M3–M5; all required before M6)

- [x] **T15 [H]: Provision host** *(done 2026-06-12: Oracle verification FAILED — the PAYG reclamation exemption is gone from the current FAQ/docs; builder call = AWS EC2 on expiring free-tier credits, t3a.medium us-east-1, EIP 98.91.67.226, provisioned via CLI under builder authorization; baseline hardening run by builder; credits $134.65 expire 2026-10-29, so the Hetzner CX23 migrate-vs-pay decision lands ~mid-Oct 2026 — full trail + evidence in `infra/host.md`. Login/OS baseline codified idempotent as `infra/provision-host.sh` (AWS+Hetzner portable) so the Oct migration re-applies in one run)*
- [x] **T16: Production runtime hardening** *(done 2026-06-14: open decision resolved — **Docker Compose process sandbox + host nftables egress** (`infra/runtime.md` records why over systemd; egress stays off the container layer because Docker's iptables churn fights host nftables). Artifacts: `infra/Dockerfile` (multi-stage, non-root `node` uid 1000, no secrets baked — `.dockerignore` excludes `.env*`/`.wa-session`, migrations copied as the dist sibling `migrate.ts` resolves); `infra/docker-compose.prod.yml` (project `hh-assistant`: `ezra` with `read_only` rootfs + `cap_drop:[ALL]` + `no-new-privileges` + tmpfs `/tmp`, the wa-session volume as the ONLY app-writable persistent surface; `postgres` deliberately not read-only — sensitive surface is the `pgdata` volume, port UNpublished, on an `internal:true` network with no internet; secrets via `env_file: ../.env` at runtime). **Egress allowlist is a unit-tested source of truth, not a static config file:** `src/ops/egress-allowlist.ts` (typed list + `isHostAllowed` dot-boundary matcher) with `tests/unit/egress-allowlist.test.ts` (11 tests) scanning `src/` for outbound host literals — a new dep that dials an unlisted host turns CI red. Covers model/embeddings/calendar/tracing/alerts/deadman/whatsapp + the reserved T17 B2/R2 slot. `infra/egress/render-allowlist.ts` (bare-node, spikes-style) emits the list; `infra/egress/nftables.sh` renders it into a default-deny forward-chain ruleset (DNS + established + resolved-CDN sets, log+drop the rest; `refresh` re-resolves on a timer). Verified HERE: build + lint clean, 420 unit tests green (+11), `docker compose config` validates with hardening directives intact. Deferred to T45 BY NECESSITY (host nftables can't run on the dev Mac): the on-host "process runs hardened" + "blocked egress to a non-listed host confirmed" drill — T16 ships the artifacts + the unit-tested allowlist, T45 confirms enforcement on the real box. `pgdata` co-locates journal+state+pgvector per the locked single-Postgres rule.)* — non-root service user, read-only rootfs, writable volumes only for Baileys session + Postgres data, egress allowlist v0 (Anthropic, Voyage (api.voyageai.com), Google, B2/R2, alert channel, WhatsApp iterated), secrets injected at runtime. Files: `infra/` (compose + egress allowlist), `src/ops/egress-allowlist.ts`, `tests/unit/egress-allowlist.test.ts`. Verify: process runs hardened; blocked egress to a non-listed host confirmed *(on-host portion is the T45 drill)*. *(Layer note 2026-06-12: this is the application RUNTIME sandbox only; the LOGIN/OS host baseline — `hh` user, SSH lockdown, patching, hostname — is codified in `infra/provision-host.sh` from T15.)*
- [x] **T17: Backup pipeline + restore drill (SPEC Phase-0 gate)** *(done 2026-06-15: **PITR to AWS S3** — builder chose S3 (compute already on AWS, free credits). Base backups (`pg_basebackup -Ft -z -Xstream`) + continuous WAL archiving (`pg_receivewal` + a physical replication slot — client-side, no `archive_command` footgun), each artifact **age-encrypted client-side, asymmetric** (host holds only the public recipient, so a host compromise can't decrypt existing backups; private key offline, restore-only), to bucket `hh-assistant-backups-001467466089` (us-east-1, same account as the EC2 host) locked down: public-access block, SSE-S3, versioning, TLS-only policy, lifecycle expiry (WAL 14d / base 35d / MPU 7d). Files: `infra/backup/{lib,backup,restore}.sh` + `Dockerfile` (pg17-client+aws+age sidecar) + `docker-compose.backup.yml` (overlay) + `README.md` (runbook); `.env.example` + `src/ops/egress-allowlist.ts` (B2/R2 slot → real S3 host, drift test updated). **Restore drill PASSED** (`restore.sh drill`, self-asserting + self-contained: spins an ISOLATED source Postgres so it touches no shared DB, drives the real backup.sh over the source's local socket — no `pg_hba` edit anywhere — writes a baseline row, base-backs-up, writes a **post-base** sentinel that exists ONLY in archived WAL, ships it, restores into a scratch pgvector container via archive recovery, asserts the post-base row survived = PITR proven, not a snapshot; cleans up containers + S3 on exit). Gotchas burned in (spike-results + README): `pg_basebackup -Xstream` issues its own WAL switch at backup end so the post-base write lands in the NEXT segment (drill waits for that exact segment in S3 — deterministic, no sleep race); the receiver must stream continuously from BEFORE the base or a gap swallows writes; archive recovery REQUIRES a `restore_command` even with WAL in pg_wal (serve staged archive via `cp`, base's bundled WAL is `cp -n` fallback that never shadows it). Verified here: build + lint clean, 421 unit tests green (+1). **Open T45 wiring item:** production `postgres` needs a replication `pg_hba` line for the sidecar — the stock image only trusts replication from localhost; the drill sidesteps it via the local socket. Restore is Postgres-only by design — Baileys session is re-paired on loss, never restored (SPEC "Never"))* — WAL archiving + base backups, client-side encrypted, to S3; restore into a scratch DB and diff. Files: `infra/backup/`. Verify: documented successful restore. Depends on: T15.

## M3 — Durable core (stubbed model, stubbed transport)

- [x] **T18: Structured store schema v0** *(done 2026-06-09: 0001-structured-store-v0.sql + forward-only runner (`pnpm migrate`; CI applies via the integration suite); store accessors take a Queryable so T19 can run them inside datasource transactions)*
  - Acceptance: migrations for `lists`, `reminders`, `household_facts` (with secret-class flag), `pending_actions`, `sent_log`, `conversation_context`; migration runner wired into dev/CI setup.
  - Verify: migrations apply cleanly in CI; store round-trip tests.
  - Files: `migrations/*.sql`, `src/memory/store.ts`, `tests/integration/store.test.ts`.
  - Depends on: T8 (validated DBOS/Postgres setup).

- [x] **T19: Step helpers — transactional writes + idempotency keys** *(done 2026-06-10: `registerTransactionalStep` (wraps a store accessor in a datasource transaction — the sanctioned write path) + `deriveIdempotencyKey(workflowID, stepNumber)`; unit tests for both + integration gate: kill-mid-flight exactly-once write, same-wfid no-re-apply, derived-key send dedupe. Gotcha for future DBOS test files: each DBOS-launching test file must pin its own `DBOS__APPVERSION` (see `tests/integration/helpers/pin-appversion.ts`) — vitest runs files in parallel and recovery claims any matching-version pending workflow)*
  - Acceptance: helper enforcing every structured-state write goes through a DBOS transactional step (SPEC boundary); `(workflowID, stepNumber)` idempotency-key helper for external effects; both unit-tested.
  - Verify: `pnpm test`; exactly-once write test (kill around the step, state neither lost nor doubled).
  - Files: `src/orchestration/steps.ts`, `tests/integration/steps.test.ts`.
  - Depends on: T18.

- [x] **T20: Ingestion seam — durable-enqueue-before-ack** *(done 2026-06-10: Zod inbound contract + `createIngestion` (validate → echo-filter → durable enqueue → ack; no ack on failure — redelivery is the retry) + stub transport honoring redeliver-unacked-on-reconnect; `Transport.onMessage` gained the explicit ack callback (Baileys passes a documented no-op until M6 wires real socket acks). Deviation from acceptance as written: echo filter keys on sent-message ids, NOT `fromMe` — personal-number deployment makes builder messages fromMe (see types.ts note); `ingestWorkflowId(messageId)` is the dedupe anchor, proven in the crash-after-enqueue-before-ack integration test)*
  - Acceptance: inbound message contract (Zod); stub transport implementing the `Transport` interface with explicit ack callback; ingestion enqueues durably *then* acks; self-echo (`fromMe`) filtered; crash-between-receive-and-enqueue test shows redelivery path (un-acked message not lost).
  - Verify: integration tests including the crash window.
  - Files: `src/orchestration/ingest.ts`, `src/transport/stub.ts`, `tests/integration/ingest.test.ts`.
  - Depends on: T11 (interface), T19.

- [x] **T21: Conversation queue + consumer-side debounce** *(done 2026-06-10: `conversation_inbox` table (migration 0002, approved in-session) is the durable rendezvous — ingest inserts exactly-once (UNIQUE message_id absorbs redeliveries) then enqueues a drain workflow per message on a partitioned concurrency-1 queue (`partitionQueue: true`, partition key = conversationId; registered after launch per 4.19.x). The partition serializes drains: the first absorbs the burst via a silence-window quiet loop over journaled reads, stragglers no-op on an empty inbox — no cross-drain coordination, no races. `groupIntoBatches` (pure, unit-tested): consecutive same-sender human bubbles merge, proactive items stay singleton, seq = the total order (enqueue order, per decision 2's ordering note — concurrent enqueues land in nondeterministic call order and tests must not assume otherwise). T22 plugs in as the drain's `processBatch` dep; T23 enqueues proactive items through the same `enqueueConversationItem` workflow. Note for T24: one unreproduced flake of T19's kill-mid-flight test observed under triple-suite load (1 in ~9 runs) — watch for it in the recovery gate)*
  - Acceptance: one concurrency-1 DBOS queue keyed by conversation; dequeue groups a sender's rapid bubbles within the silence window (1.5–3s, configurable) into one batch; FIFO across human + proactive items.
  - Verify: integration tests — debounce grouping, no pre-enqueue holding, FIFO interleave with a proactive job.
  - Files: `src/orchestration/queue.ts`, `src/orchestration/debounce.ts`, `tests/integration/queue.test.ts`.
  - Depends on: T20.

- [x] **T22: `handleTurn` skeleton + context persistence** *(done 2026-06-10: `makeHandleTurnWorkflow` (factory naming matters — the extended determinism lint rule keys on `make*Workflow`) + `context.ts` Zod message schemas/`toModelMessages`. Loop invariants gated by integration tests: every `tool_use` answered (deny continues the loop, park breaks fire-and-fold + writes `pending_actions`), `MAX_ROUNDS` (default 8 per SPEC open Q2) forces a no-tools final and throws if the forced final still calls tools, transcript never journaled whole (asserted directly against `dbos.operation_outputs`; the load step's starting-transcript output is sanctioned by the architecture pseudocode), kill-mid-tool recovery replay completes with each effect exactly once. `callModel` is a plain-function dep wrapped in `DBOS.runStep` (transcript passes by closure); load/persist/runTool deps must be pre-registered steps/transactions. New conventions.md quirk: bare-node child entries value-importing src need `ts-ext-hooks.ts` + dynamic import)*
  - Acceptance: the SPEC loop shape with stubbed `callModel` (scripted tool calls); `loadContext`/`persistContext` steps; every `tool_use` answered incl. deny/park paths; `MAX_ROUNDS` cap with forced final message; msgs never journaled whole.
  - Verify: integration tests for loop invariants; recovery replay on the skeleton.
  - Files: `src/agent/handle-turn.ts`, `src/agent/context.ts`, `tests/integration/handle-turn.test.ts`.
  - Depends on: T19, T21.

- [x] **T23: Scheduled reminders → proactive turns** *(done 2026-06-10: `tz.ts` (Intl two-pass, no deps; DST gap → pre-transition, ambiguity → first occurrence, both test-locked) + `makeReminderSweepWorkflow` enqueuing proactive items through T21's enqueue workflow — firing id = reminder id + due instant anchors child workflowID AND inbox message_id, so replays/racing ticks are exactly-once (gated: repeated sweeps, FIFO behind a slow in-flight turn, real every-second cron). Sweep boundary types are plain JSON (`dueAtIso`, `asOfMs`) because step outputs replay through the journal where Dates degrade to strings. New dbos.md gotcha: `registerScheduled` needs an already-registered workflow, raw functions never run. Production cron cadence + make-up mode decision deferred to M6 wiring)*
  - Acceptance: DBOS scheduled workflows that enqueue proactive turns into the same lane; reminder times anchored to household timezone (Eastern), never server time.
  - Verify: integration test — scheduled job waits behind an in-flight turn; tz conversion unit tests.
  - Files: `src/orchestration/scheduled.ts`, `tests/integration/scheduled.test.ts`, `tests/unit/tz.test.ts`.
  - Depends on: T21.

- [x] **T24: M3 reliability suite (`pnpm test:recovery`)** *(done 2026-06-10: the script runs the named gate files as one suite — steps (kill mid-flight exactly-once write, derived-key dedupe), queue (debounce grouping, FIFO), handle-turn (recovery replay diff, no double effect), scheduled (proactive FIFO behind in-flight turn), recovery (NEW: `src/hitl/pending-actions.ts` guarded transitions; approved→executed single-winner claim co-commits with the effect — duplicate approvals gated sequential + concurrent + deny-blocks). CI runs `pnpm test:recovery` explicitly after `pnpm test`. Suite-shakeout found and fixed a 4.19.8 launch-ordering bug (launch-time recovery vs datasource init — permanently errors recovered workflows; see dbos.md, incl. ⚠ production note for M6): kill-drill children isolate under `DBOS__VMID`, parents resume post-launch via `DBOS.resumeWorkflow`)*
  - Acceptance: the named gate tests in one suite — recovery replay (kill mid-flight, diff vs uninterrupted, no double effect), exactly-once state write, execute-once pending-action guard under duplicate approvals (table + guard land here even though full HITL is M5), debounce grouping, FIFO ordering.
  - Verify: `pnpm test:recovery` green in CI.
  - Files: `tests/integration/recovery.test.ts`, `src/hitl/pending-actions.ts`, `package.json`.
  - Depends on: T22, T23.
  - **Gate M3 complete.**

## M4 — Reasoning layer *(full acceptance criteria refined at milestone entry)*

- [x] **T25: `callModel` via AI SDK Core + prompt caching (per T7's verdict); model-call step records assistant msg + tool_use ids atomically** *(done 2026-06-10: `makeCallModel` + `toSdkMessages` in `src/agent/call-model.ts`, 12 unit tests on `MockLanguageModelV3`. Atomicity is structural — one returned `AssistantMessage` = one `runStep` journal entry, test-locked. Gotchas: `exactOptionalPropertyTypes` rejects passing `ToolSet | undefined` to optional SDK params (conditional spread); V3 mock tool-call parts take stringified `input`, result-level `toolCalls[].input` comes back parsed. `tools` stays optional until T26's registry; composing caller owns provider instantiation (`createAnthropic` from Config) — wired at the M4 gate's `pnpm dev`)*
  - Acceptance *(refined 2026-06-10 at M4 entry)*: `makeCallModel(deps)` produces the `HandleTurnDeps['callModel']` function T22 wraps in `DBOS.runStep` — deps are `{ model, systemPrompt, tools?, onUsage? }` (DI; provider instantiation stays with the composing caller via Config, never module-level). Maps the persisted `TurnMessage[]` to AI SDK messages with the stable system prefix as a system message carrying `cacheControl: ephemeral` through provider passthrough (T7 verdict; `allowSystemInMessages`); tool-result messages resolve `toolName` from prior assistant tool calls and fail loud on an unresolvable id (corrupt transcript). `forceFinal` ⇒ `toolChoice: 'none'`. Returns ONE `AssistantMessage` carrying the response text + every tool_use id/name/args — that single object is the journaled step output, which *is* the atomicity requirement. Usage (incl. cache read/write) surfaces via `onUsage` for T31/T33, never into the transcript.
  - Verify: unit tests with `MockLanguageModelV3` (`ai/test`) — no network; real model calls never run in CI.
  - Files: `src/agent/call-model.ts`, `tests/unit/call-model.test.ts`.
  - Depends on: T22 (seam), T7 (caching verdict).
- [x] **T26: Tool registry — `defineTool` (Zod schema, risk tier, idempotency, revalidation hooks)** *(done 2026-06-10: `define-tool.ts` + `registry.ts`, 14 unit tests. Confirm-before-needs-revalidate enforced at definition time; `makeRunTool` forces `parked: true` on the park path regardless of the park impl; actionId = `act-<conversationId>-<toolUseId>` — journaled values only, replay re-derives identically with no DBOS runtime read. Type gotcha: `ToolDefinition` declares `execute`/`revalidate` in METHOD syntax on purpose — bivariant params keep concretely-schemed defs assignable to `AnyToolDefinition`; arrow-property syntax breaks the registry under strictFunctionTypes. Deferred: execute errors propagate (transaction rolls back, turn errors) — turn-level error folding lands with T27's real fallible tools; `revalidate` is carried but only invoked at T35)*
  - Acceptance *(refined 2026-06-10 at M4 entry)*: `defineTool` per the SPEC snippet — name, description, Zod schema, risk tier (autonomous / notify-after / confirm-before per architecture decision 10), optional `externalId(ctx)` (deterministic from actionId), optional `revalidate(args, deps)`, `execute(args, deps, ctx)`; the SPEC "Always" boundary *confirm-before ⇒ revalidation check declared* enforced at definition time (throws). `makeToolRegistry` rejects duplicate names. Two projections: `toToolSet` → AI SDK definitions-only ToolSet (no execute — DBOS owns the loop) feeding T25's `tools` dep; `makeRunTool(registry, deps)` → the `(db, call, conversationId) → ToolResult` body the composer wraps in `registerTransactionalStep` (T22 fixture pattern), which parses args through the tool's schema, derives `actionId` deterministically from journaled values only (conversationId + toolUseId — replay-safe without DBOS runtime reads), resolves `externalId`, and dispatches by tier: autonomous/notify-after execute in-turn against the transaction-scoped db; confirm-before NEVER executes — it goes through an injected `park` seam (T34 brings the production park; the synthetic pending result shape is the seam's contract). Model-mistake paths (unknown tool, schema-invalid args) return error `ToolResult`s, never throw — every tool_use gets a tool_result. `revalidate` is carried, not invoked (runs at T35's approval execution, not propose time). Execute errors propagate (transaction rolls back) — turn-level error folding deferred to T27 when real fallible tools land.
  - Verify: unit tests (pure logic + injected fake db/park) — definition invariant, duplicate rejection, ToolSet projection has no execute, dispatch/validation/park paths, deterministic actionId.
  - Files: `src/tools/define-tool.ts`, `src/tools/registry.ts`, `tests/unit/define-tool.test.ts`, `tests/unit/registry.test.ts`.
  - Depends on: T25 (ToolSet consumer), T19 (transactional-step pattern), T24 (pending-actions guard downstream).
- [x] **T27: Tools — lists, reminders, household facts read/write** *(done 2026-06-10: eight autonomous tools (`lists.ts`/`reminders.ts`/`facts.ts`, assembled in `index.ts`), 17 integration tests through `makeRunTool` against real Postgres + full suite green incl. recovery gates. All five hard criteria test-locked: EDT 07:00→11:00Z AND EST 07:00→12:00Z stored instants; secret-class values absent from both get_fact AND set_fact result content; guarded scheduled→cancelled (new `cancelReminder`/`getScheduledReminders` accessors); Hebrew/English round-trips. Ids in result content are the follow-up contract (`reminder <uuid>`, `id <uuid>` — tests regex them out as the model would). `list_reminders` renders Eastern wall time via Intl inside the tool (step context — journaled, determinism rule doesn't apply). uuid-validated id args turn model mistakes into invalid-args tool_results instead of Postgres cast errors aborting the turn)*
  - Acceptance *(refined 2026-06-10 at M4 entry)*: the v1 tool surface (architecture "v1 tool surface" note), every tool through T26's `defineTool` with a declared risk tier — all eight are **autonomous** (reversible household-internal DB rows; no cost, no third party — per decision 10's classification axes; calendar in M5.5 is where confirm-before appears): `add_list_item` / `get_list` / `mark_item_done`; `create_reminder` / `list_reminders` / `cancel_reminder`; `set_fact` / `get_fact`. Hard criteria, each test-locked: **(1) tz anchoring** — `create_reminder` takes household wall-time fields (year/month/day/hour/minute, Eastern implied) and converts via T23's `wallTimeToInstant`; stored `due_at` asserted equal to the named instant for an EST date AND an EDT date (offset actually applied, not server-time). **(2) Secret-class read enforcement** — `get_fact` on an `is_secret` row returns an existence acknowledgment that withholds the value (the schema comment's "enforcement lives in the read paths" lands here); the value string must not appear in the ToolResult content. **(3) Guarded cancel** — `cancel_reminder` flips only `scheduled` → `cancelled` (new guarded store accessor, same shape as `markReminderFired`); cancelling a fired/cancelled reminder reports failure, doesn't throw. **(4) Round-trip through the real path** — every tool exercised through `makeToolRegistry` + `makeRunTool` against real Postgres (integration suite), state asserted in the DB, not against fakes. **(5) Mixed Hebrew/English fixtures** — list items and fact values round-trip code-switched text intact. Model-facing result content includes the ids follow-up calls need (item ids in `get_list`, reminder id in `create_reminder`/`list_reminders`). Sender attribution (`addedBy`/`createdBy`) arrives as a model-supplied arg — the model reads it from sender-attributed user messages (T25); plumbing the raw sender through the seam is deliberately not done (eval T38 checks the model gets it right). Deferrals: recurrence stays schema-only (the T23 sweep doesn't reschedule recurring reminders yet — exposing it would half-work; M6 decision); execute-error folding moves to T40 where genuinely fallible external tools appear (these tools' only failure mode is the DB, which fails the whole turn anyway).
  - *Amended 2026-06-11:* hard criterion (2) — secret-class read enforcement — retired. The user-facing secret-fact class was removed (migration `0003-drop-fact-secret-class.sql`, rationale in `docs/adr-0001-remove-secret-fact-class.md`): `is_secret` dropped from schema/store/tools, the `isSecret` tool arg removed, and the four secret-class tests deleted. The other four criteria stand.
  - Verify: `DATABASE_URL=… pnpm test` — `tests/integration/tools.test.ts` green in CI; unit tests for pure formatting/validation.
  - Files: `src/tools/lists.ts`, `src/tools/reminders.ts`, `src/tools/facts.ts`, `src/tools/index.ts` (registry assembly), `src/memory/store.ts` (cancel + list-scheduled accessors), `tests/integration/tools.test.ts`.
  - Depends on: T26, T23 (tz), T18 (store).
- [x] **T28: Pull-only semantic recall tool (pgvector) + embedding write path** *(done 2026-06-11: provider = voyage-4-lite via zero-dependency fetch client per docs/adr-0002 (criteria + cost math there); migration 0004 `semantic_memories` with `vector(1024)` + unique `source_key`; `src/memory/embedder.ts` (Embedder seam + Voyage client, dimension drift rejected client-side), `src/memory/semantic.ts` (writeSemanticMemory idempotent on source_key — the compaction-replay guarantee; searchSemanticMemories nearest-first cosine), `src/tools/recall.ts` `recall_history` (autonomous, top-k no threshold, household-local dates) — first real entry through the HouseholdToolDeps seam. Two primitives deliberately not fused: embedding is external I/O and must not run inside the insert transaction; T29 sequences them as separate steps. 7 unit tests on the wire contract (stubbed fetch), 9 integration tests incl. Hebrew code-switched recall through makeRunTool against real pgvector)*
  - Acceptance *(refined 2026-06-11 at task start)*: semantic store write idempotent on a caller-supplied source key (replay = no-op, test-locked); recall is pull-only through a registered autonomous tool returning dated, nearest-first content; query/document asymmetric embedding (`input_type`); fake embedder in CI — the real wire is exercised once by `spikes/voyage-embed.ts`; Hebrew/English code-switched fixtures throughout.
  - [x] **Real-wire smoke** *(done 2026-06-11: key added, spike PASS — 1024-dim confirmed, Hebrew query ranked the code-switched doc 0.4505 vs 0.1419; verdict recorded in docs/spike-results.md)*
- [x] **T29: Compaction step (threshold per Open Q3 proposal: ~30 turns; idempotency-keyed; open commitments kept verbatim)** *(done 2026-06-11: Open Q3 resolved — threshold 60 messages / keep 20 (`defaultCompactionConfig`); summary travels as a user message with reserved senderId `system:compaction` (no schema change). `src/agent/compaction.ts` owns the pure logic — the cut MUST land on a user boundary so a tool_result can never orphan (unit-swept across window shapes; no boundary ⇒ skip, never corrupt) — plus `makeSummarize` (Haiku-class via composer; prompt demands open commitments verbatim, Hebrew-stays-Hebrew, prior-summary folding). Workflow ordering is load-bearing: full transcript persists BEFORE compaction (a failing summarizer errors loud but can never brick the chat — test-locked), then summarize step → embed step → `writeSemanticMemory` keyed `compact-<workflowID>` → truncated persist. Kill drill (own fixture/appversion `hh-compaction-v1`, child executor pattern) SIGKILLs between the semantic write and the truncation: replay yields exactly one memory row and the identical compacted transcript; joins `pnpm test:recovery`. Chained compaction test-locked. Accepted edge: a crash there can leave the NEXT turn writing one overlapping summary row — benign in a lossy store. 10 unit + 7 integration tests; also fixed a latent T28 test-isolation bug (stale same-vector rows across runs of semantic.test.ts)*
  - Acceptance *(retrofitted 2026-06-11 from the done note — criteria as built, all already test-locked)*: threshold/keep per the Open Q3 resolution (60 messages / keep 20, `defaultCompactionConfig`); the summary re-enters the transcript as a user message with reserved senderId `system:compaction` (no schema change). The cut MUST land on a user boundary so a tool_result can never orphan — swept across window shapes; no valid boundary ⇒ skip compaction, never corrupt. Workflow ordering is load-bearing: the full transcript persists BEFORE any compaction step, so a failing summarizer errors the workflow loud but can never brick the chat; then summarize step → embed step → `writeSemanticMemory` keyed `compact-<workflowID>` (idempotent on T28's source_key — the replay guarantee) → truncated persist. Kill drill: SIGKILL between the semantic write and the truncation replays to exactly one memory row and the identical compacted transcript; part of `pnpm test:recovery`. Chained compaction folds the prior summary into the next. Summarizer prompt demands open commitments verbatim and Hebrew-stays-Hebrew. Accepted edge (documented, not fixed): a crash in that window can leave the NEXT turn writing one overlapping summary row — benign in a lossy store.
  - Verify: unit sweep on cut/threshold logic; integration suite incl. the kill drill (`tests/integration/compaction.test.ts`, fixture appversion `hh-compaction-v1`).
  - Files: `src/agent/compaction.ts`, `tests/unit/compaction.test.ts`, `tests/integration/compaction.test.ts`.
  - Depends on: T22 (workflow seam), T28 (semantic write + embedder).
- [x] **T30: Haiku router (cheap-vs-reasoning model selection)** *(done 2026-06-11: `src/agent/router.ts` — `defaultRouterPolicy` routes cheap (Haiku-class) for fresh turns and forced-final formatting; escalates to reasoning (Sonnet-class) when 2+ tool-result rounds signal chained complexity. `makeRoutedCallModel` pre-builds per-tier callModel instances and passes the tier to `onUsage` so T31 traces can tag cost by tier. 8 unit tests, 201 total green)* *(**removed 2026-06-11 — docs/adr-0003**: even with the per-turn signal fixed (amendment below), v1 drops tiered turn routing — the cheap model still makes the early tool decisions before any depth signal can fire, and a mid-turn tier switch forfeits the switched-to model's prompt cache. Every turn call (incl. forced-final) is Sonnet-class via `makeCallModel`; T33 measures the worst case. Haiku-class stays for classification (T29 summarize, T36 relatedness). `router.ts` + tests deleted; the `deps.callModel` seam is the reintroduction point if T33 fails)*
  - Acceptance *(retrofitted 2026-06-11 from the done note; amended same day after review)*: `defaultRouterPolicy` selects cheap (Haiku-class) for fresh turns and for `forceFinal` formatting calls; escalates to reasoning (Sonnet-class) at 2+ tool rounds **in the current turn** — counted as assistant-messages-with-tool-calls after the last user message (a parallel multi-call round counts once). *Amended 2026-06-11:* review caught the original policy counting `tool` messages over the whole persisted transcript — one tool-heavy turn (or one parallel two-call round) made escalation sticky for the rest of the conversation, silently defeating the cost routing; fixed with per-turn round counting + two regression fixtures (prior-turn tool history, parallel-call round). `makeRoutedCallModel` pre-builds one callModel per tier and presents the same `HandleTurnDeps['callModel']` contract — a drop-in at the T22 seam, so the workflow stays tier-blind and routing inputs are journaled values only. `onUsage` receives the tier alongside usage so T31 traces and T33 cost math can split spend per tier. Policy is injectable (custom override test-locked); code-switched Hebrew/English context routes the same as English.
  - Verify: unit tests on policy selection, tier dispatch, tier-tagged usage, custom policy (`tests/unit/router.test.ts`).
  - Files: `src/agent/router.ts`, `tests/unit/router.test.ts` — both deleted by ADR-0003.
  - Depends on: T25 (callModel factory), T22 (seam contract).
- [x] **T31: Langfuse tracing on every step; verify no operational credentials (API keys, tokens) reach traces** *(rescoped 2026-06-11: the user-facing secret-fact class was removed — docs/adr-0001 — so there is no fact-value redaction to do; what remains is verifying the by-construction credential boundary holds in emitted traces)* *(done 2026-06-11 except real-wire smoke: `src/ops/tracing.ts` (makeTracer: onModelUsage / traceRunTool / traceStep taps, never-throw on every path) + `src/ops/langfuse-sink.ts` (zero-dep batch ingestion client per ADR-0002 precedent — no new dependency). 21 unit tests incl. the credential sweep across all five secret classes and a documented-limit test: error strings copy verbatim, so clients must keep key material out of error messages (embedder.ts already does). Spans are metadata-only — no transcript content in v1 traces. `deriveActionId` exported from registry so tracer and runTool share one id derivation. Sink is deliberately lossy on flush failure (traces are observability, never load-bearing); item-level 207 errors fail loud so T33's numbers can't silently skew. ops→agent/tools imports are type-only — no runtime coupling. Wiring into the composed turn lands with T32's `pnpm dev` (no composing caller exists yet, same deferral as T25's provider)*
  - [x] **Real-wire smoke [H]** *(done 2026-06-11, run by Claude under builder authorization: PASS — flush accepted, trace verified through the Langfuse read API (generation with cache_read usage + tool span + ERROR span all render); verdict in docs/spike-results.md. Found+fixed en route: the spike died under bare node — tracing.ts value-imports deriveActionId (.js specifier), now loaded via the ts-ext-hooks child-entry pattern. US-region projects need LANGFUSE_BASE_URL=https://us.cloud.langfuse.com (config defaults to the EU host))*
  - Acceptance *(refined 2026-06-11)*: a tracing seam in `src/ops` injected through `deps` (no module-level client; Langfuse keys leave `Config` only at the composing caller). Every `DBOS.runStep`/transactional step in the turn emits a span: model-call rounds tagged with T30's tier and full usage **including cache read/write tokens** (the SPEC success criterion "cache reads visible in Langfuse traces" lands here), tool steps tagged with tool name + risk tier + actionId, compaction summarize/embed steps. Trace callbacks must never throw into the workflow (a throw fails and retries the step — same rule as `onUsage`). Credential boundary verified by test, not assertion-by-construction alone: a captured fake exporter runs a scripted turn exercising model + tool + compaction paths and the serialized trace payloads are asserted to contain none of the secret values in `Config` (Anthropic/Voyage/Langfuse keys, `DATABASE_URL` credentials). Langfuse never in CI: unit tests run against the captured exporter; the real wire is exercised once via `pnpm dev` and the observed trace (incl. nonzero cache-read tokens) recorded in `docs/spike-results.md`.
  - *Amended 2026-06-11 (ADR-0003):* the per-tier cost tag is gone with the router — model-call spans carry usage only (cache tokens unchanged); call-site step names (`callModel`, `summarizeContext`, `embedSummary`) remain the attribution.
  - Verify: unit tests with a captured exporter (span coverage + credential-absence sweep); one manual real-wire run recorded.
  - Files: `src/ops/tracing.ts`, `tests/unit/tracing.test.ts`, touch points in the composing caller.
  - Depends on: T25 (`onUsage` usage shape), T27/T29 (steps being traced). *(T30 tier-tag dependency removed by ADR-0003.)*
- [x] **T32: System prompt + mixed Hebrew/English fixture set; pending-actions digest injection slot** *(done 2026-06-11 except manual dev run: `src/agent/prompts.ts` — byte-stable `stableSystemPrompt` (sender-prefix convention, Hebrew/English matching, Eastern wall-time, tools-as-truth, recall_history, system:compaction framing) + inert digest slot (`PendingActionDigestEntry` + renderer, T34 fills it); prefix-byte stability across digest variations test-locked. `src/dev/scripted-day.ts` — four code-switched conversations; a unit test holds the `covers` union equal to the registry, so a new tool can't land unscripted. `src/dev/main.ts` + `pnpm dev` — THE composing caller: Config → createAnthropic(cheap/reasoning ids, new optional env knobs) → makeRoutedCallModel → registry projections → T31 tracer (sink flushed on exit) → compaction deps → registered handleTurn, scripted day driven turn-by-turn with replies out the stub transport. Park is a stub (all v1 tools autonomous; T34 brings the real one). Composition typechecks + 12 new unit tests; 316 total green)*
  - [x] **Manual dev run [H]** *(done 2026-06-11, run by Claude under builder authorization — three runs, final run dev-*-mqadcmm0 clean: every v1 tool exercised happy-path, replies sensible and code-switched correctly in both languages, traces in Langfuse with nonzero cache reads on every generation (covers T31's cache-read criterion). Two real behavior fixes shaken out: (1) the stable prompt now states both senders are the two members and household data is shared with no secrecy — Sonnet had refused a member's request for the stored gate code; (2) fixture sender id builder@wa renamed husband@wa — the model read "builder" as a building contractor and withheld the gate code from an "outside party" even after fix 1 (twice). Production-prompt JID mapping added to the ledger for T42. Full account in docs/spike-results.md)*
  - Acceptance *(refined 2026-06-11)*: production system prompt covering household context (two named users, sender attribution read from sender-attributed user messages — the T27 contract), tool-use guidance for the v1 surface, reply-in-the-user's-language for code-switched Hebrew/English, and Eastern-timezone framing for anything time-shaped. **Cache-prefix discipline is the hard criterion:** the static prompt is T25's stable cache prefix, so it comes first and the pending-actions digest slot appends strictly AFTER it — asserted by a unit test that the static prefix bytes are identical across digest variations (empty digest ⇒ slot omitted entirely). Note T30 means two tiers ⇒ two independent caches; both get the same stable-prefix treatment. The digest slot ships as a contract, inert in M4: `(pendingActions) => string | null`, rendered into the post-prefix slot — T34 supplies real rows. Fixture set: scripted mixed Hebrew/English conversations covering every v1 tool happy-path, consumable by the `pnpm dev` stub loop and reusable by T33's scripted days and T38's evals. **This task owns the composition wiring:** add the `pnpm dev` entry that composes `Config` → Anthropic provider → `makeRoutedCallModel` → registry assembly (`toToolSet` + `makeRunTool`) → compaction deps → `handleTurn` against the dev DB with the stub transport (no such script exists yet; the M4 gate cannot run without it).
  - *Amended 2026-06-11 (ADR-0003):* single turn tier ⇒ ONE prompt cache (the two-tiers note above is historical); composition wires `makeCallModel` (Sonnet-class) directly in place of `makeRoutedCallModel`. Prefix-stability criteria unchanged.
  - Verify: unit tests for prompt assembly + prefix stability; scripted `pnpm dev` conversation exercising at least one tool round-trip end-to-end.
  - Files: `src/agent/prompts.ts`, `src/dev.ts` (pnpm dev entry), fixtures under `tests/fixtures/` or `evals/fixtures/`, `tests/unit/prompts.test.ts`.
  - Depends on: T27 (tool surface), T25 (cache prefix mechanics). *(T30 dependency removed by ADR-0003 — composition uses `makeCallModel` directly.)*
- [x] **T33 [H]: Cost gate — measure per-turn cost on realistic scripted days, extrapolate ≤ $30/mo; cache reads visible in traces** *(done 2026-06-11, run by Claude under builder authorization: **PASS — $1.62/mo extrapolated, 18× under the $30 ceiling.** Scripted day dev-*-mqadcmm0: 9 turns / 18 generations Sonnet-only; 48,727 input tokens of which 41,388 (85%) cache reads, 3,812 cache writes, 3,527 uncached; 1,122 output. Day cost $0.0541 → per turn $0.0060 → ×30 = $1.62/mo at Sonnet 4.6 published pricing ($3/$15 per MTok, $3.75 cache write, $0.30 cache read). Margin statement: 5× volume ≈ $8/mo. Measurement note: AI SDK usage.inputTokens is the TOTAL incl. cache tokens (verified in pinned provider source). Stated caveats: no compaction fired, no proactive turns, no multi-tool chain turn in the script (a chained turn ≈ 2× per-turn cost — immaterial at this margin; T38 adds chained scenarios). Full table in docs/spike-results.md)*
  - Acceptance *(refined 2026-06-11)*: define a "scripted day" from T32's fixtures — a realistic household day's volume (reminders created/fired, list add/check/done, facts Q&A, at least one multi-tool chain that escalates the router, code-switched throughout) — and run it through `pnpm dev`. Per-turn cost computed from T31's traced usage at current published per-token pricing, **split by tier** (T30's tier tag exists precisely for this): record cheap vs reasoning token share and cost share, plus cache-read/write tokens — nonzero cache reads on the stable prefix are a pass requirement, not an observation. Extrapolate: scripted-day cost × 30 days ≤ $30/mo with the margin stated. Numbers recorded here in TASKS.md (or `docs/`) per the M4 gate line. If the gate fails, the named levers — router policy thresholds, compaction threshold/keep, prompt size — get a documented decision before M5 starts, not a silent retune.
  - *Amended 2026-06-11 (ADR-0003):* measured Sonnet-only — there is no tier split or tier tag anymore; the multi-tool chain stays as coverage but nothing escalates. Levers if the gate fails: compaction threshold/keep, prompt size; the contingency lever is reintroducing a cheap turn tier per ADR-0003 (route on the per-turn round index from `handleTurn`).
  - Verify: builder-run ([H]); the recorded numbers + cache-read evidence are the artifact. Never in CI.
  - Depends on: T31 (usage in traces), T32 (fixtures + dev loop).
  - **Gate M4 complete** ✅ *(2026-06-11: every tool exercised through `pnpm dev` stub conversations (run dev-*-mqadcmm0); T33 numbers recorded above and in docs/spike-results.md. Runs executed by Claude under builder authorization — reply transcripts in the session log for builder review.)*

## M5 — HITL *(draft criteria added + accepted 2026-06-11, then hardened same day by an audit against architecture decision 10, T24's implemented guards, and the T20 inbound contract; named entry decisions: prompt-message-id schema addition (T34, ask-first), stale-action terminal status (T35, ask-first), multi-pending ambiguity routing (T36))*

- [x] **T34: Fire-and-fold park path (synthetic pending result, `pending_actions` row, approval prompt as closing message)** *(done 2026-06-12: migration 0005 approved ask-first — nullable `prompt_message_id` on `pending_actions`, stamped post-send (`setPromptMessageId`); Open Q1 resolved 12h via `APPROVAL_TTL_HOURS` (default 12, written into expires_at by `src/hitl/park.ts`). `makeRunTool` stamps `actionId` on every parked result; `handleTurn` appends ONE deterministic approval prompt per parked action (`renderApprovalPrompt`, journaled values only — replay regenerates identical bytes), so multiple outstanding actions each get their own quotable message. Digest live: `loadPendingDigest` journaled step → per-call digest rides as a SECOND system message with no cacheControl — pinned provider keeps per-message cache_control inside one system array, so the stable prefix block's cache survives digest changes; prefix-stability tests extended, still green. Composition: `pnpm dev` runs real park + digest + `sendApprovalPrompts` (sends unstamped pending rows, stamps receipts — 'unstamped row' is the durable to-send marker, so a crash between turn and send retries, an at-least-once shape T43 formalizes). Kill-mid-park drill (`tests/integration/park-recovery.test.ts`) joined `pnpm test:recovery`: replay → exactly one unstamped pending row + one prompt, mixed autonomous+confirm-before round folds correctly. Verified against fake confirm-before tools — no v1 tool parks until T40, so `pnpm dev` can't exercise it live. Accepted v1 trade-offs: prompt/digest summaries are tool name + raw-args JSON (per-tool human rendering lands with T40) and the prompt template is English-only (deterministic workflow render can't language-match; revisit at T40 if it grates). 322 tests green)*
  - Draft acceptance *(audited 2026-06-11 against architecture decision 10 + T24's implemented guards)*: park writes the `pending_actions` row (action_id, conversation_id, serialized tool_call, status='pending', expires_at — the row shape architecture d10 names; **expires_at is written here, so Open Q1's TTL default (propose 12h) resolves at THIS task**, not T37) + the synthetic pending tool_result via transactional steps; the turn closes with an approval prompt identifying the action, and **the prompt's outbound message id is persisted with the action** — that id is what T35's quoted-reply binds to (`quotedMessageId` already exists in the inbound contract; the persistence needs a schema addition → migration is **ask-first at task start**). Multiple outstanding actions are supported (d10: quoted-reply disambiguates "even when more than one is outstanding"). In a round mixing autonomous + confirm-before calls, every tool_use still gets its tool_result and the turn ends after the round folds (T22's park break). Replaces T32's stub park in the composition. Digest slot goes live: rendered ONLY from status='pending' rows, read inside a journaled step (workflow determinism), strictly post-prefix; prefix-stability unit test keeps passing. Kill-mid-park replays to exactly one row + one prompt (joins `pnpm test:recovery`). Verified against a fake confirm-before tool — the real one is T40.
- [x] **T35: Quoted-reply approval binding + fresh-turn execution (status transitions from T24, revalidation, result injected as new context message)** *(done 2026-06-12: entry decision approved ask-first — stale actions get a NEW terminal status `stale` (migration 0005→0006 CHECK widen) so "timed out unanswered" and "approved but no longer valid" never conflate. Binding: `getActionByPromptMessageId` (conversation-scoped, status-blind) + `interpretApprovalReply` — deterministic Hebrew/English yes-no keyword sets, no model in the approve/deny decision; hedged/off-script replies are `unclear` and degrade to a normal turn with the action untouched (T36 owns conversational routing of refinements). `makeResolveApprovalReply` is ONE transactional body: bind → markApproved/markDenied (T24 guards) → first real `revalidate` call site (failure → stale, never executes; unparseable stored calls also → stale, never throw) → claimForExecution co-committing with the tool effect. Single-winner re-proven through this full path sequentially AND concurrently on raw connections — row guards alone, no queue assist. `handleTurn` gains the optional `resolveApproval` seam; quoted batch items resolve BEFORE the digest read (digest reflects what just settled) and the journaled outcome renders deterministically (`renderApprovalOutcome`) into a `system:hitl` user message — never a second tool_result (decision 10 transcript note); stable prompt extended to teach system:hitl semantics + never re-run settled actions (one-time cache-prefix re-warm, accepted). Either spouse can approve incl. proposer — nothing inspects sender; approver id is attributed in the outcome message. Kill-mid-approval-execution drill joined `pnpm test:recovery`: SIGKILL inside the resolver's execute → rollback leaves pending + zero effects → explicit resume replays to exactly one executed row, one effect, one outcome message. Composition wired in `pnpm dev` (inert until T40's confirm-before tool). English-only outcome template — same accepted T34 trade-off, revisit at T40. 393 tests green)*
  - Draft acceptance *(audited 2026-06-11)*: a quoted reply to the approval prompt resolves action_id via T34's persisted prompt-message-id (unresolvable quotes degrade to a normal turn, never throw). **Either spouse can approve — locked v1 policy (architecture d10), including the proposer.** Approval flips the T24 guard pending→approved; execution happens in a fresh turn **through the same conversation queue** (one lane — never a side channel); the approved→executed single-winner claim co-commits with the tool effect, so double approval (sequential AND concurrent — both spouses can answer back-to-back) no-ops on the guard, re-proven through this full path. `revalidate` invoked at execute time (first real call site — T26 carried it); on revalidation failure the action is NOT executed and the user is told — the terminal status for a stale action is an **entry decision** (reuse `expired` vs a new CHECK value; CHECK change = schema change, ask-first). The real outcome enters the fresh turn as a NEW context message ("action X approved and done: <result>") — never a second tool_result for the already-answered tool_use (d10's transcript note; API errors on a double answer). Deny: pending→denied + the user is told. Joins `pnpm test:recovery`. Depends on: T34.
- [x] **T36: Relatedness classifier (Haiku) — refine / unrelated / approve-deny routing while actions pend** *(done 2026-06-12: entry decision approved — multi-pending never guesses: a non-quoted message classifies only when EXACTLY one action pends; with several it stays a normal turn and a new stable-prompt "Approvals" section has the model ask for a quoted reply (second one-time cache re-warm, accepted). One documented extension beyond the criteria's letter: a quoted-but-UNCLEAR reply (bound, but not a clean yes/no) classifies against the action its quote bound, at any pending count — T35's done-note explicitly handed "conversational routing of refinements" here, and the quote IS the explicit target, so this honors binding-takes-precedence rather than weakening it. Classifier: `src/agent/relatedness.ts` — injected Haiku-class model (makeSummarize pattern; CI scripts the seam, zero CI model calls), conservative JSON verdict contract where hedged/conditional ⇒ unrelated because a wrong approve performs a real action while a wrong unrelated merely asks again; parse is fail-safe — malformed/unknown/refine-without-args all degrade to unrelated, never auto-deny, never throw. Routing: approve/deny → `makeResolveClassifiedDecision`, the T35 resolver body extracted into a shared settle core (conversation-scoped by action id; full path re-proven: guard flips, revalidation, claim+effect co-commit, already-resolved on settled rows). Refine → `makeRefineAction`: full replacement args Zod-validated against the tool schema (invalid ⇒ untouched), guarded swap while status STAYS pending (pre-execution, no idempotency hazard), same tool_use id/name so the action keeps its identity, `prompt_message_id` cleared so T34's unstamped-row marker re-sends + re-stamps the updated proposal; the turn closes like a park — deterministic re-prompt from the journaled refine outcome, rounds 0, no model call; round-trip proven (refined action approves and executes with the NEW args). Gating test-locked: 0 pending ⇒ classifier never invoked (no cost); bound quoted approve never consults it. Classifier context = last-6-message rendered tail snapshot (pure, pre-batch). Fixtures: `evals/fixtures/relatedness.ts`, every class in Hebrew/English/code-switched form — coverage asserted in CI, ACCURACY is T38's report-only job. Composition wired with real Haiku via cheapModelId (inert until T40). Accepted v1 edges: a batch's first refine ends the turn (later batch items get answered next turn) and the refine turn skips compaction. 429 tests green, recovery suite 46)*
  - Draft acceptance *(audited 2026-06-11)*: classifier is a step with an injected model dep (fakes in CI, never a real call in CI), invoked **only when ≥1 action is pending AND the message is not already a bound quoted-reply** (explicit binding takes precedence over classification; no pending actions ⇒ no classifier call, no cost). Routing — approve/deny → T35's transitions; refine ("make it 4pm") → updates the pending row's serialized tool_call **while status stays pending** (pre-execution, so no idempotency hazard) + re-prompts with the updated proposal; unrelated → normal turn with the action untouched — **never silently auto-deny** (architecture d10 names this explicitly; silence is T37's job). Ambiguity with multiple pending actions and no quote (e.g., a bare "yes") is an **entry decision** — propose: route to the single pending action when exactly one is outstanding, otherwise ask. Code-switched Hebrew/English fixtures for every class. Accuracy measured at T38 (report-only — accepted v1 risk), not CI. Parallelizable after T34.
- [x] **T37: TTL GC + gentle expiry surfacing** *(done 2026-06-12: `markExpired` is a guarded pending→expired transition — terminal, pending-ONLY (expiry can never overwrite a settled action, a racing approval beats the sweep cleanly), and an expired action can never execute: re-proven through the full T35 path for BOTH entry points (quoted reply AND classified decision report already-resolved/expired, zero effects). Sweep: `src/hitl/expiry.ts` `makeExpirySweepWorkflow`, the T23 pattern verbatim — scheduled time as workflow input (body never reads the clock), enqueue-then-mark ordering, firing id `expire-<actionId>` (action id alone — a terminal transition fires at most once ever) anchoring idempotency end to end: sweep replays and racing cron ticks collapse onto the same enqueue workflowID + inbox message_id, proven against the real scheduler with an every-second cron racing direct sweeps. The gentle notice (`renderExpiryNotice`, deterministic `[action update]` line: not approved in time, quietly expired, NOTHING was executed, ask again if still wanted) rides the proactive lane from `system:hitl` as a new `actionUpdate` payload shape — deliberately not a `text` payload, so it is structurally invisible to the T36 classifier and the quoted-reply binder (a system-produced update can never settle an action; locked at turn level). Expired rows drop out of T34's digest by construction (it renders only status='pending') — test-locked. TTL consumed from T34's Open Q1 resolution (`APPROVAL_TTL_HOURS`, 12h default, written into expires_at at park time); no new config, no migration ('expired' was in the CHECK from T24). Production composition (cron cadence + registration) lands with the other sweeps at T42/T45 — ledger #3 updated. 444 tests green, recovery gate 52)*
  - Draft acceptance *(audited 2026-06-11)*: expiry is a guarded transition (pending→expired only; the T24 approve guard already blocks approving a non-pending row, so an expired action can never execute — test-locked through the full T35 path); sweep is a scheduled workflow on the T23 pattern, replay-safe (firing id anchors idempotency); expired actions drop out of T34's digest (it renders only status='pending'); expiry message rides the proactive lane at-least-once, gently worded. TTL value is configurable and comes from T34's Open Q1 resolution (12h proposed) — this task consumes it, T34 sets it. Depends on: T34.
- [x] **T38: Eval harness (`pnpm eval`) + the five SPEC scenarios incl. code-switched fixtures** *(done 2026-06-12, gate run by Claude under builder authorization: **all 8 specs PASS first try; classifier accuracy 24/24.** Harness: `evals/harness/runner.ts` is dev/main.ts's composition with an eval registry = household surface + an eval-only `propose_event` confirm-before tool (the v1 surface is all-autonomous until T40, and the decision-9 scenarios are about the approval path — SPEC's own representative tool snippet, against an in-memory fake calendar: countable effects, flippable `isFree` for the stale scenario, idempotent create on externalId, no DB schema change for a test double). Strict YYYY-MM-DD/HH:MM Zod formats so refine verdicts are exactly checkable. Scenarios in `evals/fixtures/decision9.ts`, T32 scripted-day format (husband@wa/wife@wa per the builder@wa lesson; explicit dates so assertions don't depend on the run date; CI structure-checks coverage incl. code-switching). Driver: `quotesPrompt` messages bind to the conversation's latest stamped prompt; parked turns send prompts through the stamping path. All assertions on STATE: row status, calendar effect counts, the system:hitl context message, exactly one tool_result per tool_use, and Eastern tz anchoring re-proven at eval level through the model's own create_reminder args (8am June 25 EDT → 12:00Z due_at). Entry points spread deliberately: approve = quoted T35 binding; deny = non-quoted classified Hebrew; refine = code-switched Haiku refine with complete args + quoted כן on the RE-stamped prompt executing time=16:00; double approval = both spouses quoted back-to-back, one execution. Accuracy report-only, 24/24 incl. all six hedged/conditional traps → unrelated. Cost ≈ $0.15/full run at T33 rates; one gotcha burned in a comment — vitest 4's default reporter swallows console.log from PASSING tests, the report writes via process.stdout.write. Recovery files all green in the same sitting's full-suite run (47 files / 460 tests); CI green)*
  - Draft acceptance *(audited 2026-06-11)*: the five decision-9 scenarios — approve-after-delay, deny, abandon-by-unrelated-message, refine-the-pending-action ("make it 4pm"), stale-action-at-execution — all pass, where **pass is asserted on STATE (row status, effect counts, the new context message), never on reply wording** (model-in-the-loop output is nondeterministic; state assertions are what testing.md's "test outcomes, not internals" means here). Execute-once under double approval asserted at eval level too. Classifier-accuracy fixture set is **report-only** (accepted v1 risk per architecture; the five scenarios are the gate, accuracy is the dashboard). Includes the T27 deferral: sender-attribution correctness (the model passes the right addedBy/createdBy). Reuses T32's scripted-day fixture format — and the T32 lesson: fixture sender ids must read as unambiguous household members. On-demand only, never CI. Harness scaffolding can start parallel to T35–T37; the scenarios need the features.
  - **Gate M5 complete: PASS 2026-06-12** — all five scenarios pass + execute-once under double approval (`pnpm eval`, 8/8); recovery suite green (all seven `test:recovery` files passed in the same sitting's full run).

## M5.5 — Calendar *(parallel after T26)*

- [x] **T39 [H]: Google Cloud project + service-account credential; both personal calendars shared to the agent (ADR-0004 — supersedes the OAuth-consent wording)** *(done 2026-06-12: console done, key stored secret-class via `.env` → config; spike `spikes/calendar-sa.ts` PASSES the full create / idempotent-409 / read / delete drill on BOTH calendars. Wife's share needed two fixes: no share reads as 404 on create, a read-only share ("See all event details") as 403 `requiredAccessLevel` — both loud, never silent; "Make changes to events" applied within a minute (see spike-results). Builder to delete the downloaded key file `~/Downloads/shem86-*.json` — `.env` is the working copy)*
  - Refined acceptance *(2026-06-12, decided at T39 entry — ADR-0004)*: GCP project with the Calendar API enabled (no billing); service account created and ONE key downloaded, stored secret-class via `.env` → `config.ts` (never committed; `.env.example` documents the vars); each spouse shares their own existing calendar with the SA email, permission "Make changes to events" (no new Google account, no new calendar — per-requester routing decided 2026-06-12); config carries the sender→calendarId map the T40 tool's `owner` arg resolves through (T27 attribution contract; real JIDs land at T42 with ledger #12). Verified by spike `spikes/calendar-sa.ts`: RS256 JWT via node:crypto → token endpoint → create+read+delete round-trip on BOTH calendars, verdict in `docs/spike-results.md`. Zero new deps (ADR-0002 fetch-client pattern). Builder does the console + sharing clicks; everything else scripted.
- [x] **T40: Calendar tools — deterministic event IDs from action_id, confirm-before tier, revalidation (slot still free)** *(done 2026-06-12, 6 slices, all criteria met: `src/tools/calendar-client.ts` (zero-dep client, JWT verified in-suite against a generated keypair, token cache, CalendarRequestError, isFree ignores all-day events + our own id) and `src/tools/calendar.ts` (create confirm-before + list autonomous, Eastern anchoring EST+EDT test-locked, 409→success no-op, human `summarize`). Surface extensions: `revalidate` gained the id context; `summarizeToolCall` is the ONE proposal renderer — handleTurn's closing prompt (new `summarizeProposal` seam), digest, and sendApprovalPrompts render identical bytes. Ledger #5 resolved (builder-approved): settle-core throw → new 'failed' outcome, row returns to pending in-transaction (`returnToPending` guard), re-approval retries — proven exactly-once after a transient 503. `makeV1ToolRegistry` composes household+calendar; `pnpm dev` wires the real client (sole credential holder). Evals deliberately untouched (T38 green+paid) — swapping eval `propose_event` for the real tool shape is a T41/M6 follow-up. Integration lock `tests/integration/calendar-approval.test.ts`: park→human prompt→quoted approve→one event with `hh`+sha256(action_id), conflict→stale, outage→failed/pending→retry-once, replay execute→already-exists no-op. Zero new deps, no migration, CI never touches Google. 506 tests green)*
  - Refined acceptance *(2026-06-12, decided at T40 entry; builder approved failure-mode + surface)*: surface = `create_calendar_event` (**confirm-before**, d10: third-party-visible) + `list_calendar_events` (**autonomous** read), both through `defineTool` with an `owner` arg (`husband`|`wife`, requester's calendar unless they say otherwise — ADR-0004 routing via `config.calendarIds`, model fills it from T27 sender attribution). Args use the T38-proven household-wall-time formats (`date` YYYY-MM-DD, `time` 24h HH:MM, `durationMin` default 60), conversion inside the tool via `wallTimeToInstant`; test-locked for an EST and an EDT date. **(1) Deterministic event id** = `hh` + SHA-256 hex of `action_id` (hex ⊂ Google's base32hex alphabet); re-create returns the spike-proven **409, folded to success** — the recovery-replay no-op and the T44 reconciliation handle. **(2) Revalidation** = target owner's slot still free via events.list over the window, **ignoring an event that carries our own deterministic id** (else a crash between POST and commit makes the replay call its own event a conflict and wrongly stale the action) — `revalidate` widens to optionally receive the id context; existing two-arg implementations stay valid. **(3) Zero-dep client** (`src/tools/calendar-client.ts`): spike JWT/token logic graduates with injected `fetch`, token cached to near-expiry; constructed from config by the composer so tools get an authenticated client through `deps`, never the key; JWT unit-verified with `node:crypto` against a generated keypair — CI never touches Google. **(4) Ledger #5 lands — execute-error folding**: the read tool folds fetch/HTTP errors into an error tool_result; on the approval path a failed revalidate/execute is caught in the settle core and the row **returns to `pending` in the same transaction** (transient outage ≠ stale; no new status, no schema change), with a deterministic 'failed — still pending, approve again to retry' outcome message; TTL still bounds the action's life and the idempotent id makes retry safe even if the first POST landed. **(5) Per-tool human rendering** (T34/T36 deferral): optional `summarize` hook on the definition; approval prompt + digest render the event humanly, JSON fallback stays; English-only template stays (accepted T34 trade-off). **(6) Composition**: `makeV1ToolRegistry()` = household + calendar (deps-contravariance layer `CalendarToolDeps`); `pnpm dev` wires the real client; **evals stay untouched** (T38 is green and paid — swapping `propose_event` for the real tool shape is a T41/M6 follow-up note). Zero new deps, no migration; park→approve→execute round trip proven in the integration suite against real Postgres with a fake client. Real-calendar verification is T41's gate.
- [x] **T41: Round-trip gate — create/read on a test calendar; re-execute no-ops; manufactured conflict caught** *(done 2026-06-12: `spikes/calendar-roundtrip.ts` PASS first run — production client + real tool execute/revalidate against the real API; 66-char production id accepted/returned verbatim (first real-wire contact for the sha256 derivation), real 409 → already-exists fold, timed+all-day parse and rendering, Eastern anchor round-trips (07:30 EDT → 11:30Z), manufactured conflict caught + own-id exemption + all-day non-blocking, self-cleaning exit. Verdict in spike-results. Zero new deps, zero src changes — the gate found nothing to fix. **M5.5 complete.** Eval propose_event→real-shape swap deferred to M6 per builder call)*
  - Refined acceptance *(2026-06-12, decided at T41 entry; builder approved venue + eval deferral)*: one on-demand gate script `spikes/calendar-roundtrip.ts` (`node --env-file=.env spikes/calendar-roundtrip.ts`; real calendar writes — never CI) driving the **production** stack: `makeGoogleCalendarClient` from config plus the real `create_calendar_event.execute`/`revalidate` and `list_calendar_events.execute` with injected deps — no model, no DB (the approval machinery is T40's integration lock; model-in-the-loop is T38). Venue = the husband's calendar at a far-future date: precheck aborts if the slot isn't empty; cleanup deletes ONLY the event ids this run created (delete stays off the production client on purpose — the agent surface gets no delete; the script mints its own token for setup/cleanup, spike-style). Proves on the real wire: **(1)** the production 66-char `hh`+sha256 event id is accepted and returned verbatim by Google — the first time the real derivation touches the API (the T39 spike used a different one); **(2)** re-execute → real 409 → folded to already-exists success through the production status path (the recovery no-op); **(3)** `listEvents` Zod parse against real responses, timed AND all-day shapes, incl. the list tool's rendering; **(4)** Eastern anchoring round-trips — Google returns exactly the instant `wallTimeToInstant` produced — and the durationMin default lands as a 60-min event; **(5)** manufactured conflict: `revalidate` for a DIFFERENT action over the occupied window → false; own-id exemption → true; an all-day event does NOT block the slot; **(6)** clean exit — created events deleted, window empty. Verdict recorded in `docs/spike-results.md`. Eval `propose_event`→real-tool-shape swap deferred to M6 (builder call 2026-06-12). Zero new deps; zero src changes expected.

## M6 — Wire-up and launch *(entered 2026-06-12: ledger swept, criteria refined below; requires T14 ✓, T17 ✗, M5 gate ✓ — T15→T16→T17 is the parallel critical path)*

- [x] **T42: Swap stub for the M2 Baileys adapter; durable-enqueue-before-ack against the real socket** *(done 2026-06-14 — all five smoke checks passed on real WhatsApp; progress 2026-06-12, 4 slices committed, 523 tests green: (1) ack-binding resolved — receipts non-deferrable in Baileys 7 (pinned-source verified), criterion amended with builder approval, documented in baileys.ts/types.ts; (2) production identity — `makeProductionSystemPrompt` (Ezra/עזרא, JID→member map, attribution by member label, recurrence honesty), `loadProductionConfig` (WA_JID_HUSBAND/WIFE + WA_HOUSEHOLD_CONVERSATIONS), dev cache prefix byte-identical; (3) `src/main.ts` production composition — full spine (Baileys → ingestion with conversation-allowlist privacy boundary → lane → handleTurn → reply), both sweeps registered (1-min reminders, 5-min expiry, WhenActive), health/alerts/dead-man wired, `pnpm start`; (4) launch-recovery pass — `src/start.ts` per-generation DBOS__VMID + `resumeStrandedWorkflows()` post-launch, kill-drill proven in `launch-recovery.test.ts`, added to test:recovery; dbos.md ⚠ resolved. SMOKE (2026-06-14, builder-run on real WhatsApp): Ezra replies in the household group, code-switches Hebrew/English, and **member attribution verified for BOTH members**. Two findings fixed en route — (a) production composition never logged the inbound sender JID (only the manual `runner-cli.ts` did), so the value `WA_JID_*` needs was invisible: added the `[inbound] chat=… sender=…` line to `main.ts`'s `onMessage`; (b) both `WA_JID_HUSBAND` and `WA_JID_WIFE` had been set to the SAME placeholder `@lid` (the wife's actual JID), so the husband matched no member and Ezra asked "who are you?" — corrected `WA_JID_HUSBAND` to the husband's real `@lid` read off the new log line (.env, local only — gitignored). All five checks confirmed by the builder: (1) Ezra replies in the group, (2) code-switched Hebrew/English, (3) echo suppression — the bot's own sends are not re-ingested, (4) member attribution correct for both members, (5) non-allowlisted chats rejected. T42 closed)*
  - Refined acceptance *(2026-06-12, M6 entry)*: production composition (`src/main.ts`, the dev/main.ts pattern with the real transport) wires the T11 Baileys adapter behind the SAME `Transport` interface — the T20 ingestion seam (validate → echo-filter → durable enqueue → ack) unchanged. **(1) Ingestion durability against the real socket (ledger #2 — AMENDED 2026-06-12, builder accepted):** receipt deferral is impossible in Baileys 7.x — the protocol receipt is sent lib-internally BEFORE `messages.upsert` fires (verified in pinned source, `messages-recv.js`: `sendReceipt` precedes `upsertMessage`; no public hook), so the ack callback stays a no-op on the real adapter and "ack fires after enqueue" holds only at the seam (stub-proven). The production guarantee instead: process-down windows are covered by WhatsApp's offline redelivery on reconnect ('append' upserts — adapter delivery already test-locked) + `ingestWorkflowId` dedupe making redelivery safe; the receipted-but-uncommitted window (ms-scale, in-process) is ACCEPTED — documented in baileys.ts/types.ts and again in T44's runbook. Fork-the-lib rejected (standing surgery on the riskiest dependency to close a milliseconds window). **(2) Echo suppression preserved (ledger #11):** sent-ID-based, never `fromMe` — personal-number deployment; verified with a real send. **(3) Production system prompt (ledger #12 + persona + recurrence):** names the bot **Ezra (עזרא)**, maps the two real sender JIDs → husband/wife (config-carried map, same pattern as `calendarIds`; T32 proved id semantics steer the model), states recurring reminders are NOT supported v1 (set the next occurrence; the firing message is the re-ask point — ledger #4 cut). **(4) Sweeps registered (ledger #3):** reminder sweep (T23) + pending-action expiry (T37) registered as scheduled workflows in the production composition (6-field crontab, Eastern-anchored cadence; both are state-scans with guarded flips, so missed ticks self-heal late — at-least-once by design). **(5) Recovery-race mitigation (ledger #1):** per-process-generation executor ID (`DBOS__VMID` set at spawn, read at SDK import) + explicit post-launch `DBOS.resumeWorkflow` pass over pending workflows of prior generations — the dbos.md ⚠ production wiring; proven by a kill-mid-turn → restart → turn-completes integration drill. Real WhatsApp traffic is ask-first: builder runs the manual verifications.
- [x] **T43: Send classes live — at-least-once (reminders, nags, approval prompts; send-then-log) and at-most-once (echoes; log-then-send) against `sent_log`** *(done 2026-06-14, 3 slices, 545 tests green / recovery gate 57: `src/transport/send-class.ts` carries `selectSendClass` (the unified reply step picks the class from the lead inbox item — sender `'system'` reminder/nag firing ⇒ at-least-once, human reply or low-stakes `system:hitl` notice ⇒ at-most-once, default at-most-once so an unknown sender never spams a duplicate), `replySendId`/`approvalSendId` (deterministic, lead-message-id-derived so a step replay lands on the same row), and `deliverReply` (at-most-once = claim-then-send, at-least-once = check-then-send-then-log on the existing `recordSend`/`getSentEntry` primitives). Wired into `main.ts` `processTurnBatch` via the plain `replyDb` client (autocommits the claim independent of the step journal — that independence IS the gate). Approval prompts co-commit their sent_log row + `prompt_message_id` stamp in one transaction (`recordApprovalSend`), T34 stamping byte-identical. Human jitter relocated to the pure `protocol.ts` and applied to every production send via `jitteringSend` inside the send STEPs. Kill-mid-flight gate (`send-class-recovery.test.ts`, in `test:recovery`): at-most-once never re-sends across a crash (wire stays 1), at-least-once re-sends (wire 2, the accepted duplicate) then settles to one log row. **NOT in scope (see ledger #14): durable `wasSentByBot`** — stays the adapter's in-memory `sentIds`; a restart-surviving echo guard needs a schema change or a deterministic send id, both ask-first.)*
  - Refined acceptance *(2026-06-12, M6 entry)*: every outbound send goes through a send-class wrapper keyed by a deterministic send id from `(workflowID, stepNumber)` into `sent_log` (T18 schema). **At-least-once** (reminders, nags, approval prompts): send-then-log — a crash between the two re-sends on replay (duplicates allowed by the class, each logged). **At-most-once** (conversational replies/echoes): log-then-send — a crash between the two drops the send on replay (never duplicates; the log row is the tombstone). Kill-mid-flight integration tests for BOTH orderings on both classes; approval prompts keep the T34 stamping path (`prompt_message_id` persisted in the same transaction as the log row — the quoted-reply anchor survives). Human send-jitter (T13) applies to production sends; jitter lives in a step, never the workflow body. *(Note 2026-06-14: the send id is lead-message-id-derived rather than literal `(workflowID, stepNumber)` — equivalent determinism, since the turn runs as `turn-${messageId}` and the reply is one-per-turn; simpler than reaching for the step number inside the body.)*
- [x] **T44: Recovery runbook (`docs/recovery-runbook.md`) + full restore drill incl. external-effect reconciliation** *(done 2026-06-15 — runbook written + **drill PASS** (Claude under builder authorization). The drill is automated and self-contained: `infra/backup/t44-reconcile-drill.sh` builds the runbook's exact timeline — a PRE-base `approved` pending_action + baseline at-least-once `sent_log` row → base backup (real `backup.sh` → encrypted → real S3) → POST-base effects that "already left the box" (action flips to `executed`, an at-most-once + an at-least-once `sent_log` row, and a **REAL Google event** with the production id `hh`+sha256(action_id) via the calendar leg `infra/backup/t44-calendar-effect.ts` driving `makeGoogleCalendarClient`/`deriveCalendarEventId`) → **base-only restore** (no archived WAL ⇒ recovery stops at base-end = the rewind) into a scratch pgvector container → reconcile. All three legs PASS: **§4c** action restored as `approved` (the executed flip rewound); **§4a** post-base `sent_log` rows absent, baseline survived; **§4b** the restored action's id re-derives identically, re-execute → real Google **409 → folded to already-exists** → window holds **exactly one** event (no duplicate). Blast radius contained: isolated source Postgres (no real DB), drill-scoped S3 prefix (never production `pitr/`), **ephemeral age keypair** (the production private identity stays offline — encrypt+decrypt both happen in-run, same `lib.sh` age path), far-future calendar slot prechecked empty + deleted on exit; verified zero residue (no containers, no S3, slot empty). The T17 base+WAL PITR leg was re-confirmed PASS the same day via `restore.sh drill`. Recorded in the runbook drill-record table + `docs/spike-results.md`. **No src/test changes** — the reconciliation primitives were already test-locked (`send-class-recovery.test.ts`, `calendar-approval.test.ts`, T41 real wire); T44 is the doc + drill that ties them together. The folded T45 sub-drill (d) re-runs this on the real host once the backup sidecar's `pg_hba` replication line is live; the mechanism is now proven.)*
  - Refined acceptance *(2026-06-12, M6 entry)*: runbook covers the four loss scenarios with exact commands — process crash (DBOS auto-recovery + the T42 resume pass), host loss (restore base backup + WAL into a fresh Postgres), Baileys session loss (re-pair only — NEVER restore session state from backup, SPEC "Never"), and partial external effects (DB restored to before effects that already landed). **Reconciliation is mechanical, not judgment:** `sent_log` answers "what did we already send" (at-least-once rows may re-fire, at-most-once rows must not); deterministic calendar ids answer "what did we already create" — re-executing a restored-as-approved action re-derives `hh`+sha256(action_id) and folds 409 → already-exists (T41 proved Google honors this). **Drill:** one full restore into a scratch DB, diff against live, run the runbook's reconciliation steps against `sent_log` + a calendar event created post-backup; results logged in the runbook. Depends on T17 (the pipeline being drilled).
- [x] **T45 [H]: Deploy hardened process to host; egress allowlist final** *(done 2026-06-15 — all four drills PASS on host `hh@98.91.67.226`. Hardened ezra deployed (read-only rootfs, uid 1000, cap_drop ALL), crash→recover (a), self-heal state (b), dead-man/Telegram (c) all PASS; egress allowlist enforced both directions on the real box (S3 + IMDS allowed, non-listed dropped). **Backup pipeline live + (d) host-loss restore PASS:** continuous WAL archiving + daily base cron running, and the real production backups were restored end-to-end on a different machine using the builder's offline key — full schema, 6 migrations, real rows, promoted out of recovery. PROX-SEND-001 fixed + re-drilled in situ. Full log `docs/ops-drills.md`.)*
  - Refined acceptance *(2026-06-12, M6 entry)*: the T42 composition runs on the T15 host under T16 hardening (non-root, read-only rootfs, writable volumes only for Baileys session + Postgres data, secrets injected at runtime); egress allowlist finalized and a blocked non-listed host confirmed (Anthropic, Voyage, Google, B2/R2, Telegram, healthchecks, WhatsApp). On-host drills, results in `docs/ops-drills.md`: **(a)** kill -9 mid-turn → restart → the turn completes, no duplicate effects (ledger #1 verified where it matters); **(b)** sweeps fire on live cadence, and a process stop across a reminder's due time fires it late on restart (ledger #3 self-heal observed); **(c)** T14's alert/dead-man drills re-pass from the host. **(d)** the T44 restore drill (folded in 2026-06-15): once the backup sidecar's `pg_hba` replication line is live and continuous WAL is archiving, run the real host-loss restore + external-effect reconciliation per `docs/recovery-runbook.md` (real calendar event post-backup, restore behind it, re-execute → 409 no-op), logged in the runbook's drill-record table — this is what finally closes T44. Depends on T15, T16, T42.
  - **Progress 2026-06-15 (Claude under builder authorization; full log `docs/ops-drills.md`):** host stood up end-to-end on the EC2 box (`hh@98.91.67.226`) — Docker 29.5.3 + Compose v5.1.4 + Node 22 installed, source rsynced, prod image builds (**fixed a Dockerfile `.npmrc` bug** — frozen install honored the lockfile's `auto-install-peers=false` only after copying `.npmrc`; the T16 "image builds" had never actually run), Postgres up + healthy (pgvector 0.8.2, port unpublished), host `.env` loaded from the builder's local file (POSTGRES_PASSWORD generated/preserved across first-init) + `loadProductionConfig()` validated in a throwaway container, paired (**fixed a root-owned `wa-session` volume bug** — non-root read-only-rootfs couldn't persist creds → QR loop; Dockerfile now pre-owns `/data/wa-session` as `node`), **ezra live + hardening verified** (`ReadonlyRootfs=true`, uid 1000, `cap_drop:[ALL]`), real round-trip confirmed by builder. Drills: **(a) crash→auto-restart→recover→reconnect PASS** (nuance: `docker kill`/`stop` is an intentional stop, NOT auto-restarted; only a real process death is — host-side `kill -9` proved it); **egress allowlist enforced + made persistent** via new in-repo `infra/egress/hh-egress.{service,timer}` (boot apply + 15-min refresh under the 1h nft-set TTL; builder-authorized) — **blocked non-listed host confirmed**, Anthropic/WhatsApp allowed; **(b) self-heal STATE PASS**; **(c) dead-man/Telegram PASS** (builder-confirmed). **PROX-SEND-001 RESOLVED + verified in situ (2026-06-15):** the (b) drill's proactive-send drop is fixed (`makeResilientSend` budget-based retry) and re-drilled on the host — reminder delivered with a `sent_log` row; the drill measured reconnect time varying ~12–85s across restarts (slow-tail cause not diagnosed) and the ~85s tail drove the budget sizing (`docs/ops-drills.md`). **STILL OPEN before T45 closes:** only **(d)** the folded T44 restore drill *on the real host*. **Note 2026-06-15:** T44's reconciliation drill PASSES end-to-end on the dev Mac against real S3 + the real Google wire (`infra/backup/t44-reconcile-drill.sh`; isolated source, drill-scoped prefix, ephemeral age key) — **mechanism proven**. **WAL wiring built + validated 2026-06-15:** the open replication prerequisite is now `infra/backup/enable-replication.sh` (idempotent — appends `host replication hh samenet scram-sha-256` + reloads; reuses the `hh` superuser the sidecar already carries; `wal_level=replica` PG17 default already suffices). Proven against the dev postgres: a replication connection from a separate container is **refused without the line and succeeds after**, and the prod sidecar image's continuous `receivewal` over TCP shipped a complete encrypted 16 MiB WAL segment to S3 that decrypted to exactly 16777216 bytes. Also **fixed a latent deploy blocker**: `.dockerignore` excluded all of `infra`, so the sidecar image's `COPY infra/backup/*.sh` had never built — the two scripts are re-included. **Host apply 2026-06-15 (Claude under builder authorization, SSH `hh@98.91.67.226`):** scripts rsynced to the host; **`enable-replication.sh` applied on the prod `postgres`** (pg_hba line 131 `host replication hh samenet scram-sha-256`, reloaded, `wal_level=replica` ✓); **sidecar image builds on the host** (the `.dockerignore` fix verified there). **Continuous WAL archiving is NOT yet live — two builder credential prerequisites surfaced, neither present on the host:** (1) **no AWS access** — the EC2 instance has **no IAM instance profile** (IMDS returns 404 for the role) and no keys in `.env`, so the sidecar's aws-cli can't reach S3; needs a least-priv instance role OR scoped `BACKUP_AWS_*` keys (builder/IAM call); (2) **no `BACKUP_AGE_RECIPIENT`** in `.env` — builder must generate the keypair offline (private key in a password manager, restore-only) and set the public recipient. Until both land the sidecar can't ship. **The real (d) restore drill additionally needs the offline age private key** to decrypt production backups — fundamentally builder-gated. Options + exact commands documented in `infra/backup/README.md` "Builder prerequisites". **AWS path resolved (builder pick 2026-06-15): instance profile.** A least-priv IAM role `hh-assistant-backup-ec2` (List on the bucket + Get/Put/Delete/multipart on its objects) is created + associated with `i-0a7e9f4767666ac9e`, IMDS hop limit 2, host IMDS exposes the role. **Blocked on the last mile, which needs host root (Claude lacks passwordless sudo):** the hardened egress firewall drops the container→IMDS path, so an nftables rule allowing link-local `169.254.169.254:80` was added to `infra/egress/nftables.sh` (rsynced to host) — builder runs `sudo systemctl restart hh-egress.service` to apply, then `docker run --rm --network hh-assistant_egress --entrypoint aws hh-assistant-backup:prod sts get-caller-identity` should resolve the role (if not, Docker masquerade isn't SNATing link-local → add a `meta` masquerade or fall back to scoped keys). **Still pending (builder, offline custody): `BACKUP_AGE_RECIPIENT`** generated offline → host `.env`. **So T45(d) reduces to:** builder (1) `systemctl restart hh-egress.service`, (2) sets `BACKUP_AGE_RECIPIENT` → then start sidecar (`docker compose … -f docker-compose.backup.yml up -d`) + base-backup cron → host-loss restore drill (needs the offline private key). Doc fixes en route: `runtime.md` deploy cmd needs `--env-file .env`; `render-allowlist.ts` Node-on-host rationale; V2 streamlining notes captured in `V2_NOTES.md`. **Update 2026-06-15b (Claude under builder authorization, host `hh@98.91.67.226`): AWS reachability DONE + T45 egress drill PASS on host.** Sudo recovered without a password — `hh` is `--disabled-password` in the `sudo` group so its sudo can never authenticate; the cloud-default **`ubuntu`** account still has passwordless sudo and is the working root path (scoped permanent fix codified: `infra/host/sudoers-hh-ops`). Two on-host blockers found + fixed in `infra/egress/nftables.sh`, applied + verified on the box: **(1)** the egress firewall dropped container→IMDS, so the instance role was unreadable — added the link-local `169.254.169.254:80` allow (Docker masquerade SNATs it to the instance IP, confirmed); **(2)** S3 cannot be allowlisted by DNS (its pool is huge + per-query-randomized — the SDK dialed `52.217.116.34`/`16.15.244.232`, never in the resolved set, dropped) → load AWS's **published S3 CIDRs** (`ip-ranges.json`, `service=S3 region=us-east-1`, 21 prefixes) into a dedicated interval set `allowed_nets4` (separate set is load-bearing: single host IPs overlap the CIDRs and nft rejects `conflicting intervals` within one set), refreshed on the timer, last-good cached. **Drill PASS both directions:** `aws s3 ls s3://hh-assistant-backups-001467466089/` RC=0 with instance-role creds; non-listed `1.1.1.1`/`example.com` dropped (default-deny holds). One transient fail-open during deploy (destructive `apply` deletes the table before loading; the overlap error left no table) — caught, re-applied with the two-set fix after an `nft -c -f -` dry-run; lesson baked into `nftables.sh` STATUS. SSM was tried as an alt root path but the agent isn't installed on this baseline; the managed policy attached for the attempt was **detached** (role back to S3-only). Full log: `docs/ops-drills.md`. **T45(d) now reduces to ONE builder action + the drill:** set `BACKUP_AGE_RECIPIENT` (generated offline) in the host `.env` → start the sidecar + base-backup cron → host-loss restore drill (needs the offline private key). **Update 2026-06-15c (Claude under builder authorization): continuous PITR archiving is LIVE on the host.** Builder generated the age keypair offline + gave the public recipient (`age1j4r…urws`, set in host `.env`; private identity stays in their password manager). Brought the sidecar up end-to-end: slot `hh_backup` created, `pg_receivewal` streaming, **WAL segments shipping automatically** (`…0003.age`/`…0004.age`, 16 MiB age-encrypted) and a **base backup** landed (`base.tar.gz` 4.4 MB + `pg_wal.tar.gz` + `MANIFEST`, encrypted, under `pitr/base/<ts>/`); sidecar 0 restarts. `PGPASSWORD` wired as `${POSTGRES_PASSWORD}` compose interpolation (one secret, builder pick), creds via the EC2 instance role (IMDS). **Daily base cron** installed in `hh`'s crontab (03:00 UTC). Fixed two latent compose bugs that only bite the *combined* `-f prod -f backup` invocation (the drill/standalone build masked them): the overlay's relative `context`/`env_file` paths resolve against the project dir `infra/` not the overlay's own dir (`../..`→`..`, `../../.env`→`../.env`, else they overshoot to `/home/hh`), and `--env-file .env` from the repo root is load-bearing for interpolation. Full log: `docs/ops-drills.md`. **Only remaining T45 item: the host-loss restore drill** — fundamentally builder-gated (needs the offline `BACKUP_AGE_IDENTITY` to decrypt).
- [x] **PROX-SEND-001 [launch-blocking bug, found 2026-06-15 in the T45 self-heal drill]: proactive sends dropped during the restart reconnect window** *(done 2026-06-15, fix + in-situ drill PASS, Claude under builder authorization: a pure injectable `makeResilientSend` wrapper (`src/transport/send-class.ts`) retries ONLY the transient `transport not connected` error with capped exponential backoff until a time BUDGET is spent, composed into the production send in `src/main.ts` so BOTH the reply path AND the approval-prompt path inherit it; runs inside the send DBOS step so backoff timers are journaled (elapsed = sum of slept delays, no clock read → deterministic/testable). Permanent/unroutable errors (bad jid — ledger #15) deliberately NOT matched. Repro relocated from a `deliverReply`-level `it.fails` to a real `makeResilientSend` suite + composition test (exactly-once delivery + one `sent_log` row across a transient disconnect). **Budget sizing came from the on-host drill, not a guess:** first cut used 8-attempt/63s; the re-drill measured reconnect time varying ~12–85s across restarts (slow-tail cause NOT diagnosed — plausibly WhatsApp-side and/or Baileys session resync, not proven), and the ~85s tail expired the 63s budget so the reminder still dropped — revised to `maxElapsedMs` 5min + `maxDelayMs` 5s cap (covers the ~85s worst case with ~3.5× margin, tolerating the slow tail regardless of cause). **Drill 2 PASS:** reminder `eaeffc6f` delivered with an at-least-once `sent_log` row; logs show the capped backoff + successful send post-reconnect (`docs/ops-drills.md`). Bonus finding: a dropped at-least-once proactive send self-heals via inbox re-drain on the next enqueue (inbox item never marked processed) once the transport is reachable — strengthens T44 reconciliation. 578 unit+integration green, recovery gate 57. Residual accepted: a transport down past 5min still errors the turn — the T12 health/dead-man case. Writeups `docs/known-issues.md` + `docs/ops-drills.md`)* — On restart the reminder sweep fires before Baileys reconnects → the proactive turn's send threw `transport not connected` → `drainConversation`/`processTurnBatch` went to terminal ERROR (not retried) → the reminder was marked `fired` but the message **lost** (no `sent_log` row). Hit the at-least-once class (reminders/expiry) specifically — the one the architecture says must never drop.
- [x] **T46 [H]: Launch sweep — SPEC success-criteria checklist top to bottom** *(done 2026-06-21 — `docs/launch-checklist.md` complete, all 22 SPEC boxes closed with evidence; the parked eval swap landed + `pnpm eval` GREEN 8/8; real-traffic cost re-check PASS. **M6 done — launch checklist complete.**)*
  - Refined acceptance *(2026-06-12, M6 entry)*: every SPEC Success Criteria box checked with evidence (doc/test/drill link), recorded in `docs/launch-checklist.md` — Phase-0 (caching ✓ T7, host T15, verified restore T17), functional, reliability (incl. the five decision-9 scenarios), cost (≤ $30/mo extrapolation re-checked against real-traffic traces). Includes the parked eval swap: eval registry replaces the eval-only `propose_event` with the REAL `create_calendar_event` shape (fake client stays — evals never touch Google), `pnpm eval` re-run green (paid, builder-authorized). Launch is the checklist completing, not a date.
  - **Progress 2026-06-15 (eval swap, code slice — Claude):** the parked eval swap is **implemented**. The eval registry is now `makeV1ToolRegistry()` (household surface + the production `create_calendar_event`/`list_calendar_events`); the eval-only `propose_event` tool (`evals/harness/propose-event.ts`) is **deleted**. The only stand-in left is an in-memory fake `CalendarClient` (`evals/harness/fake-calendar.ts`, reworked to implement the real interface — owner-scoped window-intersection `isFree` with `ignoreEventId` exemption, idempotent `createEvent` on the deterministic `hh`+sha256 id, `setBusy(owner, window)` for the stale scenario's manufactured conflict). `runner.ts` wires `calendarClient` through `CalendarToolDeps`, renders the digest/closing prompt through the real `summarize` hook (`summarizeProposal` + registry-aware `toDigestEntries`, matching `dev/main.ts`/prod). `decision9.eval.ts` asserts on the real shape (id via `deriveCalendarEventId`, refined time via the start instant, conflict via the derived window); fixtures (`decision9.ts` covers, `relatedness.ts` toolName/summary) and CI structure tests (`eval-harness.test.ts`, `eval-scenarios.test.ts`, `relatedness.test.ts`) updated. Verified: 432 unit tests green, eval files type-check + collect (`vitest list`) clean, build + lint clean — **NO `src/` changes**. **STILL builder-gated:** the paid `pnpm eval` re-run (green) and the `docs/launch-checklist.md` sweep with real-traffic cost re-check — the rest of T46.
  - **Progress 2026-06-15 (paid eval re-run — Claude, builder-authorized):** ran `pnpm eval` against the swapped suite. **First run 4/8**, which surfaced a **real production refine bug** (commit `91e26fa`): T40's `summarize` hook made the pending-action digest render a human line, and `handleTurn` fed THAT to the relatedness classifier instead of the raw args JSON the original T36 contract specified — so the classifier couldn't name the fields a refine patch needs, `refineAction`'s full-replacement validation failed, and a calendar refine silently became a normal turn (production had this bug, not just the eval). Fix: classifier reads raw args JSON again (`argsJson` added to the digest entry, fed by `handleTurn`); `refineAction` now MERGES the classifier patch over the stored args (so a one-field change keeps `owner`/`date`); classifier prompt asks for only the changed fields. Also reworded the abandon + double-approval scenarios so the calendar `owner` is explicit (the new required field made the model ask whose calendar instead of parking). **Second run 7/8** — refine, abandon, double-approval all PASS; relatedness accuracy 24/24 both runs. Full regression: 432 unit + 582 integration + 57 recovery green. **One failure remained, swap-independent: `sender-attribution`** — the model stored `added_by='wife'` (member label), the assertion wanted the raw `'wife@wa'`. Root cause was a **dev/prod prompt divergence**: the eval ran the DEV `stableSystemPrompt` ("pass the id as addedBy"), but `makeProductionSystemPrompt` says "pass the member label (husband/wife), never the raw id" — so the model did the production-correct thing and the dev-prompt assertion flagged it. **Resolved (builder call, ledger #16): align the eval to production** — the harness now composes `makeProductionSystemPrompt` (scenario senders mapped to members) and the assertion expects the member label, so the gate tests real launch behavior. **Third run: 8/8 PASS** (all decision-9 scenarios + execute-once double-approval), classifier accuracy 24/24, commit `ca4ed2f`. **`pnpm eval` is GREEN.** Cost ≈ $0.15/run at T33 rates. Remaining for T46: the `docs/launch-checklist.md` SPEC-criteria sweep + real-traffic cost re-check (builder-gated).
  - **Progress 2026-06-18 (launch checklist — Claude):** built `docs/launch-checklist.md` — the full SPEC "Success Criteria" sweep top to bottom, each box with its evidence link (Phase-0 / functional / reliability / cost). **21 of 22 boxes closed with evidence** (T7 caching, T15 host, T17+T45(d) verified restore; the four functional criteria via T23/T27/T28/T40/T41 + the T45 on-host reminder drill; the four reliability criteria via `test:recovery`+T45(a), T12/T14+T45(c), the `pnpm eval` 8/8 decision-9 gate, and the T17/T44/T45(d) restore+reconciliation drills; the T33 scripted cost gate at 18× under ceiling). **T46 now reduces to ONE open box: the real-traffic cost re-check** — fundamentally builder-gated (needs the production Langfuse project pulled for live traffic since the T42 launch; I can't reach it). Flagged low-risk in the checklist: cost structure is unchanged from the T33 gate (Sonnet-only, byte-identical cache prefix dev↔prod), with the exact close-out method recorded. Doc-only change, no `src/` touch. **T46 stays unchecked until that number lands.**
  - **Progress 2026-06-21 (real-traffic cost re-check — Claude, builder-paired in Langfuse): LAST BOX CLOSED, T46 DONE.** Builder walked into the Langfuse project (first time); the prebuilt dashboards only show counts, so pulled the numbers directly via the read-only `/api/public/observations` API and summed `usageDetails` over all **234 `callModel` generations**, window **2026-06-12 → 2026-06-15 (3.15 days)**: input 762,133 (cache reads **596,567 = 78%**, cache writes 112,878, uncached 52,688), output 11,547. **Caching verified active on LIVE traffic (78% reads vs T33 dev's 85%)** — the SPEC "caching active / cache-read visible" criterion now met on real traces. **Cost: $8.89/mo cache-aware; $23.42/mo on the conservative bound (caching priced as if OFF, all input at full $3/MTok)** — both under $30. Honest caveat (strengthens the pass): the window is **test-dominated** (~74 gen/day vs T33's realistic ~18/day → ~4× volume, mostly T42 smoke + T47 clock-bug bursts), so it over-states volume; at organic household volume ≈ $2/mo cache-aware / ≈ $6/mo with caching off. Closed on the conservative bound: *even at ~4× realistic volume with zero cache credit, under ceiling.* En route, chased down + **cleared a false alarm**: the first generation the builder opened showed `cache_read = 0`, which the claude-api skill flags as a silent-invalidator symptom — verified against the pinned `@ai-sdk/anthropic@3.0.82` source that consecutive system messages keep their own `cache_control` (so T47's per-turn clock block does NOT break the stable prefix's breakpoint), and the aggregate 78% read rate confirmed caching is healthy (the zero was just a first-in-window write). No code change. Non-gating post-launch item noted in the checklist: re-confirm cost on organic-only traffic once weeks of it accrue.

- [x] **T47: Push the current time into every turn (the model had no clock)** *(done 2026-06-14, surfaced at the T42 smoke. BUG: the turn model's training anchor makes it feel like "today" is ~mid-2025, and nothing in the turn assembly told it otherwise — the stable prompt is deliberately byte-stable ("no dates, no per-turn state") for the cache. So every relative time resolved ~11 months into the PAST: "remind me today at 17:07" stored `2025-07-23 17:07` (correct time-of-day since stated, stale date) and fired immediately because a past-due reminder is instantly overdue — masquerading as success; "in 5 minutes" had neither date nor time and broke outright; a calendar "tomorrow" landed `2025-07-24` (real but unfindable). DECISION (builder, evidence-backed): PUSH the clock every turn, not a pull `get_current_time` tool — the model is inconsistent about knowing it doesn't know (it asked for the reminder but silently guessed the calendar date), so a pull tool has the same "must decide to call it" gap plus a worse multi-turn UX; and pushing the date+cutoff is exactly what Claude's own system prompt does (`{{currentDateTime}}` + knowledge-cutoff line, platform.claude.com/docs/en/release-notes/system-prompts). FIX: `renderCurrentTimePrompt(now)` (prompts.ts) renders the Eastern wall-clock + weekday + a "your training is frozen ~early 2025, do not trust your own date sense" framing; `toSdkMessages`/`makeCallModel` push it as a post-prefix system block with NO cacheControl (rides the same dynamic region as the digest, clock first — cached prefix bytes proven unchanged). Clock read inside the journaled callModel STEP (not the workflow body): a completed step replays its output so the captured time is durable, an interrupted step re-runs with an equally-valid fresh read; determinism lint clean (not a workflow factory). Production uses the default real clock — no wiring needed. 558 unit+integration green, recovery gate 57.)*

## Deferred-decisions ledger *(added 2026-06-11 — threads buried in done-task notes; each resolves at the milestone named, swept at M6 entry)*

| # | Thread | Buried in | Lands at |
|---|---|---|---|
| 1 | Launch-recovery race in production — scope executor IDs per process generation or verify fixed DBOS version, else crash-restart can permanently kill the turn it should recover | dbos.md ⚠ note (from T24) | **swept into criteria 2026-06-12**: implementation in T42 (per-generation `DBOS__VMID` + post-launch resume pass), on-host crash drill in T45 |
| 2 | Real socket acks — Baileys `onMessage` ack is a documented no-op; durable-enqueue-before-ack must bind to real acks | T20 note | **resolved at T42 (2026-06-12, builder call): binding is impossible** — Baileys 7.x sends receipts internally pre-event (pinned-source verified). Offline redelivery + dedupe carry the guarantee; ms in-flight window accepted + documented (baileys.ts, T44 runbook) |
| 3 | Production cron registration for BOTH sweeps — reminders (T23) and pending-action expiry (T37); neither is registered outside test fixtures yet. Make-up half dissolved: both sweeps are state-scans with guarded transitions, so downtime self-heals on the next tick (a missed 7am reminder fires late — exactly at-least-once) | T23/T37 notes | **swept 2026-06-12**: registration in T42, live-cadence verification in T45 |
| 4 | Recurrence schema-only since T27 — expose or cut for v1 | T27 note | **resolved at M6 entry (builder call 2026-06-12): CUT for v1.** Column + firing-identity design stay dormant (no migration); T42's production prompt makes the model honest about it and offers the manual loop (set this occurrence; the firing message is the re-ask point). First post-launch feature candidate |
| 5 | Execute-error folding for fallible external tools | T26/T27 notes | **resolved at T40**: reads fold to error tool_results; approval-path throws fold to 'failed' + return-to-pending (retryable) |
| 6 | Compaction crash window can write one overlapping summary row — benign, documented | T29 note | accepted |
| 7 | T19 kill-mid-flight flake under triple-suite load (1/~9, unreproduced) | T21 note | watch in `test:recovery` |
| 8 | Open Q1: approval TTL default (propose 12h) — written into expires_at at park time, so it resolves at T34 (T37 consumes it) | SPEC | **resolved at T34**: 12h (`config.approvalTtlHours`); SPEC Q1 updated 2026-06-12 |
| 9 | Open Q4: bot persona name | SPEC | **resolved at M6 entry (builder pick 2026-06-12): Ezra (עזרא)** — lands in T42's production prompt; SPEC Q4 updated |
| 10 | Branch protection unenforced (free plan) — red CI blocks by discipline | T5 note | standing |
| 11 | Echo handling is sent-ID-based, not `fromMe` (personal number) — T42 must preserve | T11/T20 notes | **swept into T42 criteria 2026-06-12** |
| 12 | Production system prompt must name/map the two real sender JIDs to the members — phone-shaped JIDs carry no member semantics, and the T32 runs proved id semantics steer the model (builder@wa read as a contractor → gate-code refusal) | T32 done-note, spike-results | **swept into T42 criteria 2026-06-12** |
| 13 | semantic.test.ts "empty store" test races parallel suites on the shared dev DB: it DELETEs inside an open transaction, but READ COMMITTED lets later SELECTs see rows the compaction suite commits mid-test (observed 1/3 full-suite runs at T34, passes alone and on rerun) — fix is scoping the recall query/cleanup to run-unique conversation ids, same recipe the other suites use | T34 session | watch; fix if it recurs |
| 14 | Durable `wasSentByBot` (restart-surviving echo guard) — the adapter's in-memory `sentIds` (protocol.ts `RecentIds`) suppresses echoes only within a process; after a restart WhatsApp could redeliver the bot's own `fromMe` send before `sentIds` refills, and `fromMe` filtering is forbidden (personal number). `sent_log` can't answer it (keyed on idempotency key, not the WhatsApp message id an echo carries). Two ways to close it, **both ask-first**: (a) a `wa_message_id` column / sidecar table populated post-send (DB schema change), or (b) a deterministic client-side send id in the adapter so the WA id == the sent_log key (WhatsApp-adjacent adapter change). T43 left it on the in-memory guard — the named deliverable (send classes) needs neither | T43 session 2026-06-14 | **open — builder decision (schema vs adapter id); post-launch unless a restart-echo is observed** |
| 15 | Undeliverable-send poison pill — an **at-least-once** send (reminder/nag/approval prompt) to a destination the socket can't reach throws in the `sendReply`/`sendApprovalPrompts` step and, because the class never drops, retries forever, wedging that conversation's concurrency-1 lane. Surfaced at the T42 smoke when leftover TEST reminders (fake conversation ids like `conv-run-…`, no `@server`) fired from the shared dev DB and `jidDecode` threw inside Baileys `relayMessage` — root cause there was prod running against the test DB (fixed by a dedicated `hh_assistant_prod`), but the **permanent-vs-transient** gap is real for live use too (a chat deleted/blocked, a malformed jid). Fix is its own small task: classify a send error as permanent (bad/unroutable destination — never retry; dead-letter + alert) vs transient (network/5xx — keep retrying), so a poison message can't block the lane. At-most-once already tolerates the drop; at-least-once is the one that wedges | T42 smoke 2026-06-14 | **open — post-launch hardening; pre-launch mitigation is keeping prod off the test DB (done)** |
| 16 | Dev/prod prompt divergence on sender attribution — `stableSystemPrompt` (dev/eval) says "pass the **id** as addedBy/createdBy", `makeProductionSystemPrompt` says "pass the **member label** (husband/wife), never the raw id". The T38 `sender-attribution` eval asserts the raw id (`wife@wa`) but the model (correctly, for prod) stores the label (`wife`) — the only red in the T46 7/8 re-run. Decide the canonical attribution value and reconcile: (a) point the eval at the production prompt + assert the member label, or (b) align both prompts (and the assertion) on one convention | T46 eval re-run 2026-06-15 | **resolved 2026-06-15 (builder call): option (a) — eval now runs `makeProductionSystemPrompt` + asserts the member label; `pnpm eval` 8/8 (commit `ca4ed2f`). The dev `stableSystemPrompt` still says "pass the id" but the dev loop no longer gates on attribution; a full dev/prod prompt reconciliation is optional post-launch cleanup** |

## Sequencing notes

- Start with T1–T6 (scaffold batch).
- T7/T8/T9 are independent — good parallel batch after M0.
- T11 needs a paired WhatsApp number available (provisioning out of scope) — everything else in M2–M5 proceeds without it via the stub transport.
- M4 tasks stay coarse on purpose: T25's exact shape depends on T7's verdict, T29's threshold on real transcript sizes from T33's scripted days. Refine at milestone entry per the living-document rule.
- **Current frontier (2026-06-15, T45 done):** **T45 complete** — hardened ezra live on `hh@98.91.67.226`, egress allowlist enforced both directions on the real box, **continuous PITR archiving live** (sidecar streaming WAL + daily base cron, encrypted to S3 via the EC2 instance role), and the **host-loss restore drill PASSED** — real production backups reconstructed end-to-end on a different machine with the builder's offline key (full schema, 6 migrations, real rows, promoted). All four on-host drills (a)–(d) green; PROX-SEND-001 fixed in situ. En route, fixed: the egress firewall's IMDS + S3-by-published-CIDR rules (S3 can't be DNS-allowlisted — `docs/ops-drills.md`), a scoped `hh` sudoers drop-in (`infra/host/sudoers-hh-ops`), and two latent compose overlay path bugs (relative paths resolve against the project dir `infra/`). **Only remaining task: T46** (launch sweep) — also the parked eval swap: replace the eval-only `propose_event` with the real `create_calendar_event` shape. Watch rows: #7 (T19 flake), #13 (semantic test race), #14 (durable `wasSentByBot`, post-launch).
- **Prior frontier (2026-06-15, T17 done; T16 + T43 + T15 already landed):** **T17 complete** — encrypted PITR to AWS S3 (base + continuous WAL, age asymmetric client-side), bucket provisioned + locked down, **restore drill PASSED** proving a post-base WAL write survives restore (`infra/backup/`, `docs/spike-results.md`). Two open wiring items folded into T45: (a) host nftables egress enforcement (T16 artifact), (b) production `postgres` replication `pg_hba` line for the backup sidecar (T17). New ledger #14 (durable `wasSentByBot`) parked for a builder schema-vs-adapter-id call, post-launch unless a restart-echo shows up. Watch rows: #7 (T19 flake) and #13 (semantic test race) stay open-eyes. Eval follow-up still parked at T46: swap the eval-only `propose_event` for the real `create_calendar_event` shape.
- **Prior frontier (2026-06-14, T16 done; T43 + T15 already landed):** **T16 complete** — production runtime sandbox resolved to Docker Compose + host nftables (`infra/runtime.md`); hardened `infra/Dockerfile` + `infra/docker-compose.prod.yml`, and the egress allowlist is now a unit-tested source of truth (`src/ops/egress-allowlist.ts` + drift test) the nftables ruleset renders from. Its on-host enforcement drill is T45's by necessity.
- **Prior frontier (2026-06-14, T43 done; T15 landed in parallel):** **T43 complete** — send classes live on the unified reply path + approval prompts, kill-mid-flight gate added to `test:recovery` (57). **T42 CLOSED (2026-06-14):** all five real-traffic smoke checks passed — Ezra replies + code-switches in the group, echo suppression holds, both members attribute correctly, non-allowlisted chats rejected. Fixed a hidden config trap en route (both `WA_JID_*` were the same placeholder) after adding inbound-sender logging to the production composition. The whole M2→M6 production spine is now proven on real WhatsApp. **T15 done (builder — host provisioned, AWS EC2):** the host exists now, so the critical path is **T16 (runtime hardening) → T17 (backup + restore drill)**, both buildable. **Also buildable without the host: T44** (recovery runbook — its `sent_log` reconciliation view is exactly what T43 just made real); its *restore drill* still depends on T17. Then T45 (deploy, needs T16) → T46 (launch sweep). New ledger #14 (durable `wasSentByBot`) parked for a builder schema-vs-adapter-id call, post-launch unless a restart-echo shows up. Watch rows: #7 (T19 flake) and #13 (semantic test race) stay open-eyes. Eval follow-up still parked at T46: swap the eval-only `propose_event` for the real `create_calendar_event` shape.
- **Prior frontier (2026-06-12, M6 entered):** entry sweep done — ledger rows 1/2/3/11/12 folded into T42/T45 criteria, #4 resolved (recurrence CUT for v1, builder call), #9 resolved (persona = Ezra/עזרא), #8 marked resolved-at-T34; SPEC open questions 1/2/4 closed inline. Two parallel tracks: T42→T43→T44 buildable without the host; T15 [H] → T16 → T17 the builder-gated critical path.
- **M5.5 does not wait for M5.** T41's round-trip gate exercises the calendar tool's `execute`/`revalidate` directly with injected deps against a test calendar; only the approval-path end-to-end needs M5 (covered by T38/M6). T40–T41 proceed as soon as T39 lands.
- **M6 entry must sweep the deferred-decisions ledger above** — that, plus refining T42–T46 criteria, is the entry step.
